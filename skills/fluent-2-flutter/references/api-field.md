# Field: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/field/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentField`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentField`

A Fluent 2 field: the label, hint and validation message around a control.

Source: `packages/fluent_2/lib/src/inputs/field.dart`

#### Constructor: `FluentField`

```dart
const FluentField({
    super.key,
    this.child,
    this.label,
    this.hint,
    this.validationMessage,
    this.validationMessageIcon,
    this.validationState = FluentFieldValidationState.none,
    this.size = FluentFieldSize.medium,
    this.required = false,
    this.enabled = true,
    this.style,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget?` | No | `null` | The control being wrapped. |
| `label` | `Widget?` | No | `null` | The label content. Wrapped in a [FluentLabel] sized from [size]. |
| `hint` | `Widget?` | No | `null` | The hint below the control. Upstream's `hint` slot. |
| `validationMessage` | `Widget?` | No | `null` | The validation message below the control. |
| `validationMessageIcon` | `Widget?` | No | `null` | The glyph beside [validationMessage]. Tinted and sized through [IconTheme]; see [FluentFieldBaseState.validationMessageIcon]. |
| `validationState` | `FluentFieldValidationState` | No | `FluentFieldValidationState.none` | What the field is reporting about [child]'s value. |
| `size` | `FluentFieldSize` | No | `FluentFieldSize.medium` | Label ramp and label-to-control gap. |
| `required` | `bool` | No | `false` | Whether to render the required-field asterisk after [label]. |
| `enabled` | `bool` | No | `true` | Whether the field renders in its enabled colours. |
| `style` | `FluentFieldStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the field renders in its enabled colours.

## Related public types

### `FluentFieldBaseState`

Source: `packages/fluent_2/lib/src/inputs/field.dart`

#### Constructor: `FluentFieldBaseState`

```dart
const FluentFieldBaseState({
    required this.enabled,
    this.label,
    this.hint,
    this.validationMessage,
    this.validationMessageIcon,
    this.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the field renders in its enabled colours. |
| `label` | `Widget?` | No | `null` | The label above the control, if any. |
| `hint` | `Widget?` | No | `null` | The hint below the control, if any. Upstream's `hint` slot; Figma's `Helper text` row. |
| `validationMessage` | `Widget?` | No | `null` | The validation message below the control, if any. |
| `validationMessageIcon` | `Widget?` | No | `null` | The glyph beside [validationMessage], if any. |
| `child` | `Widget?` | No | `null` | The control being wrapped. Any widget: the field never inspects it. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the field renders in its enabled colours.

### `FluentFieldSize`

Label ramp and label-to-control gap. Figma's `Size` axis, and the only axis the `Field` component set has.

Source: `packages/fluent_2/lib/src/inputs/field.dart`

```dart
enum FluentFieldSize {
  small,
  medium,
  large,
}
```

### `FluentFieldState`

Source: `packages/fluent_2/lib/src/inputs/field.dart`

#### Constructor: `FluentFieldState`

```dart
const FluentFieldState({
    required super.enabled,
    required this.size,
    required this.validationState,
    super.label,
    super.hint,
    super.validationMessage,
    super.validationMessageIcon,
    super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the field renders in its enabled colours. |
| `size` | `FluentFieldSize` | Yes | — | Label ramp and label-to-control gap. |
| `validationState` | `FluentFieldValidationState` | Yes | — | What the field is reporting. |
| `label` | `Widget?` | No | `null` | The label above the control, if any. |
| `hint` | `Widget?` | No | `null` | The hint below the control, if any. Upstream's `hint` slot; Figma's `Helper text` row. |
| `validationMessage` | `Widget?` | No | `null` | The validation message below the control, if any. |
| `validationMessageIcon` | `Widget?` | No | `null` | The glyph beside [validationMessage], if any. |
| `child` | `Widget?` | No | `null` | The control being wrapped. Any widget: the field never inspects it. |

### `FluentFieldStyle`

Source: `packages/fluent_2/lib/src/inputs/field_style.dart`

#### Constructor: `FluentFieldStyle`

```dart
const FluentFieldStyle({
    this.labelPadding,
    this.secondaryTextPadding,
    this.secondaryTextStyle,
    this.hintColor,
    this.validationMessageColor,
    this.validationMessageIconColor,
    this.validationMessageIconPadding,
    this.validationMessageIconSize,
    this.gap,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `labelPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the label row. |
| `secondaryTextPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the hint row and the validation-message row. |
| `secondaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | The type ramp of the hint and the validation message. |
| `hintColor` | `WidgetStateProperty<Color?>?` | No | `null` | Hint text colour. |
| `validationMessageColor` | `WidgetStateProperty<Color?>?` | No | `null` | Validation message text colour. |
| `validationMessageIconColor` | `WidgetStateProperty<Color?>?` | No | `null` | Validation message glyph colour. |
| `validationMessageIconPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset above the validation message glyph. |
| `validationMessageIconSize` | `WidgetStateProperty<double?>?` | No | `null` | Validation message glyph edge length. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the validation message glyph and its text. |

### `FluentFieldTheme`

Overrides the field style for a subtree.

Source: `packages/fluent_2/lib/src/inputs/field.dart`

#### Constructor: `FluentFieldTheme`

```dart
const FluentFieldTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentFieldStyle` | Yes | — | The style layered over the size and validation-state defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentFieldValidationState`

What the field is reporting about the value of the control it wraps.

Source: `packages/fluent_2/lib/src/inputs/field.dart`

```dart
enum FluentFieldValidationState {
  none,
  error,
  warning,
  success,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentFieldState resolveFluentFieldState({
  bool enabled = true,
  FluentFieldSize size = FluentFieldSize.medium,
  FluentFieldValidationState validationState = FluentFieldValidationState.none,
  bool required = false,
  Widget? label,
  Widget? hint,
  Widget? validationMessage,
  Widget? validationMessageIcon,
  Widget? child,
});

FluentFieldStyle resolveFluentFieldStyle(
  FluentFieldState state,
  FluentThemeData theme,
);

Widget buildFluentField(
  FluentFieldBaseState state,
  FluentFieldStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`:

```dart
FluentField(
              label: FluentLabel(child: Text('First name')),
              hint: Text('Enter your name'),
              child: FluentInput(),
            )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/inputs/field.dart`
- Tests: `packages/fluent_2/test/goldens/field_golden_test.dart`, `packages/fluent_2/test/inputs/field_test.dart`
- Stories: `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/field/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
