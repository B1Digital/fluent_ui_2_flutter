# Tree: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/tree/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentTree`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentTree`

A Fluent 2 tree: nested, expandable rows with one indent step per level.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTree`

```dart
const FluentTree({
    super.key,
    required this.items,
    this.appearance = FluentTreeAppearance.subtle,
    this.size = FluentTreeSize.medium,
    this.selectionMode = FluentTreeSelectionMode.none,
    this.openItems,
    this.defaultOpenItems = const <Object>{},
    this.onOpenChange,
    this.selectedItems = const <Object>{},
    this.onSelectionChange,
    this.onInvoke,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `items` | `List<FluentTreeItem>` | Yes | — | The root nodes. |
| `appearance` | `FluentTreeAppearance` | No | `FluentTreeAppearance.subtle` | Fill treatment, applied to every row. |
| `size` | `FluentTreeSize` | No | `FluentTreeSize.medium` | Row height and type ramp, applied to every row. |
| `selectionMode` | `FluentTreeSelectionMode` | No | `FluentTreeSelectionMode.none` | Which selection control the rows carry. |
| `openItems` | `Set<Object>?` | No | `null` | The open set. Non-null makes the tree controlled; the widget then never changes it itself and [onOpenChange] must be handled. |
| `defaultOpenItems` | `Set<Object>` | No | `const <Object>{}` | The initial open set for an uncontrolled tree. |
| `onOpenChange` | `ValueChanged<Set<Object>>?` | No | `null` | Reports the next open set. Required when [openItems] is non-null. |
| `selectedItems` | `Set<Object>` | No | `const <Object>{}` | The selected set, driving both the selection control and the `Selected` token ramp. |
| `onSelectionChange` | `ValueChanged<Set<Object>>?` | No | `null` | Reports the next selected set. Null renders the selection control disabled. |
| `onInvoke` | `ValueChanged<Object>?` | No | `null` | Invoked when a row is activated by click, Space or Enter, in addition to whatever the activation did to the open set. |
| `style` | `FluentTreeItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the name of the tree. |

#### State, callback, and accessibility fields

- `openItems` (`Set<Object>?`): The open set. Non-null makes the tree controlled; the widget then never changes it itself and [onOpenChange] must be handled.
- `onOpenChange` (`ValueChanged<Set<Object>>?`): Reports the next open set. Required when [openItems] is non-null.
- `selectedItems` (`Set<Object>`): The selected set, driving both the selection control and the `Selected` token ramp.
- `onSelectionChange` (`ValueChanged<Set<Object>>?`): Reports the next selected set. Null renders the selection control disabled.
- `onInvoke` (`ValueChanged<Object>?`): Invoked when a row is activated by click, Space or Enter, in addition to whatever the activation did to the open set.
- `semanticLabel` (`String?`): Announced by assistive technology as the name of the tree.

## Related public types

### `FluentTreeAppearance`

How a tree row is filled. Figma's `Style` axis.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

```dart
enum FluentTreeAppearance {
  subtle,
  subtleAlpha,
  transparent,
}
```

### `FluentTreeCollapseIntent`

Closes the focused branch, or ascends to its parent when it is already closed.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeCollapseIntent`

```dart
const FluentTreeCollapseIntent();
```

### `FluentTreeEdgeIntent`

Moves focus to the first or last visible row.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeEdgeIntent`

```dart
const FluentTreeEdgeIntent({required this.last});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `last` | `bool` | Yes | — | Whether to go to the last row rather than the first. |

### `FluentTreeExpandIntent`

Opens the focused branch, or descends into it when it is already open.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeExpandIntent`

```dart
const FluentTreeExpandIntent();
```

### `FluentTreeItem`

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeItem`

```dart
const FluentTreeItem({
    required this.value,
    required this.label,
    this.icon,
    this.actions,
    this.searchLabel,
    this.children = const <FluentTreeItem>[],
    this.enabled = true,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `value` | `Object` | Yes | — | Identity, used as the key in the open and selected sets. Compared with `==`, so it must be stable across rebuilds. |
| `label` | `Widget` | Yes | — | The row's label. |
| `icon` | `Widget?` | No | `null` | Optional leading icon, drawn between the chevron and the label. |
| `actions` | `Widget?` | No | `null` | Optional trailing controls. Figma's `Quick actions` slot, which holds a 24px subtle icon button — pass a `FluentButton.icon` to reproduce it. |
| `searchLabel` | `String?` | No | `null` | Plain text used for typeahead, since [label] is a widget and cannot be read. Null opts this row out of typeahead. |
| `children` | `List<FluentTreeItem>` | No | `const <FluentTreeItem>[]` | The subtree. Empty makes this a leaf, which draws no chevron and takes one extra indent step so its label lines up with its siblings'. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. Disabled is a real state: it swaps the whole colour ramp, refuses focus, and is skipped by the arrow keys. |

#### State, callback, and accessibility fields

- `value` (`Object`): Identity, used as the key in the open and selected sets. Compared with `==`, so it must be stable across rebuilds.
- `enabled` (`bool`): Whether the row responds to input. Disabled is a real state: it swaps the whole colour ramp, refuses focus, and is skipped by the arrow keys.

### `FluentTreeItemBaseState`

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeItemBaseState`

```dart
const FluentTreeItemBaseState({
    required this.enabled,
    required this.level,
    required this.branch,
    required this.expanded,
    required this.selectionMode,
    required this.selected,
    this.icon,
    this.label,
    this.actions,
    this.onSelectionChanged,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `level` | `int` | Yes | — | Depth in the tree, 1 for a root row. Both Figma's `Tree indentation` collection and upstream's `--fui-TreeItem--level` are 1-based. |
| `branch` | `bool` | Yes | — | Whether this row has a subtree. Figma's `Leaf node` axis, inverted. |
| `expanded` | `bool` | Yes | — | Whether the subtree is open. Always false on a leaf. |
| `selectionMode` | `FluentTreeSelectionMode` | Yes | — | Which selection control the row carries. |
| `selected` | `bool` | Yes | — | Whether the row is selected. Drives both the control and Figma's `Selected` state. |
| `icon` | `Widget?` | No | `null` | Optional leading icon. |
| `label` | `Widget?` | No | `null` | The row's label. |
| `actions` | `Widget?` | No | `null` | Optional trailing controls. |
| `onSelectionChanged` | `ValueChanged<bool>?` | No | `null` | Invoked by the selection control with the next value. Null renders the control disabled. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the row responds to input.
- `expanded` (`bool`): Whether the subtree is open. Always false on a leaf.
- `selected` (`bool`): Whether the row is selected. Drives both the control and Figma's `Selected` state.
- `onSelectionChanged` (`ValueChanged<bool>?`): Invoked by the selection control with the next value. Null renders the control disabled.

### `FluentTreeItemState`

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeItemState`

```dart
const FluentTreeItemState({
    required super.enabled,
    required super.level,
    required super.branch,
    required super.expanded,
    required super.selectionMode,
    required super.selected,
    required this.appearance,
    required this.size,
    super.icon,
    super.label,
    super.actions,
    super.onSelectionChanged,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `level` | `int` | Yes | — | Depth in the tree, 1 for a root row. Both Figma's `Tree indentation` collection and upstream's `--fui-TreeItem--level` are 1-based. |
| `branch` | `bool` | Yes | — | Whether this row has a subtree. Figma's `Leaf node` axis, inverted. |
| `expanded` | `bool` | Yes | — | Whether the subtree is open. Always false on a leaf. |
| `selectionMode` | `FluentTreeSelectionMode` | Yes | — | Which selection control the row carries. |
| `selected` | `bool` | Yes | — | Whether the row is selected. Drives both the control and Figma's `Selected` state. |
| `appearance` | `FluentTreeAppearance` | Yes | — | Fill treatment. |
| `size` | `FluentTreeSize` | Yes | — | Row height and type ramp. |
| `icon` | `Widget?` | No | `null` | Optional leading icon. |
| `label` | `Widget?` | No | `null` | The row's label. |
| `actions` | `Widget?` | No | `null` | Optional trailing controls. |
| `onSelectionChanged` | `ValueChanged<bool>?` | No | `null` | Invoked by the selection control with the next value. Null renders the control disabled. |

### `FluentTreeItemStyle`

Source: `packages/fluent_2_web/lib/src/navigation/tree_style.dart`

#### Constructor: `FluentTreeItemStyle`

```dart
const FluentTreeItemStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.expandIconColor,
    this.borderRadius,
    this.textStyle,
    this.padding,
    this.innerPadding,
    this.contentPadding,
    this.actionsPadding,
    this.gap,
    this.iconSize,
    this.expandIconSize,
    this.expandIconExtent,
    this.indent,
    this.rowGap,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Row fill. Fluent's `Neutral/Background/{Subtle,Transparent}/*` ramps. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label and leading-icon colour. `Neutral/Foreground/2/*`. |
| `expandIconColor` | `WidgetStateProperty<Color?>?` | No | `null` | Chevron colour. A separate ramp from [foregroundColor] — Figma binds `Neutral/Foreground/3/*` on the chevron and `2/*` on the label, and upstream's `useTreeItemLayoutStyles` says the same. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Row corner radius. `Corner radius/Medium`. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [foregroundColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the whole row, before the per-level indent is added. |
| `innerPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Vertical inset around the icon-and-label group. Figma's `Start content`, which is what actually sets the row height: 6 + 20 + 6 = 32 medium, 4 + 16 + 4 = 24 small. The row frame itself carries no vertical padding. |
| `contentPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the label alone. Figma's `Content slot`. |
| `actionsPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the trailing actions slot. Figma's `Quick actions`. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the leading icon, the selector and the label. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Leading icon box. |
| `expandIconSize` | `WidgetStateProperty<double?>?` | No | `null` | Chevron glyph box. |
| `expandIconExtent` | `WidgetStateProperty<double?>?` | No | `null` | Width reserved for the chevron, occupied by a spacer on a leaf. |
| `indent` | `WidgetStateProperty<double?>?` | No | `null` | Horizontal inset added per level of nesting. |
| `rowGap` | `WidgetStateProperty<double?>?` | No | `null` | Vertical space between rows. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum row size. Only the height is used. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor shown over an enabled row. |

### `FluentTreeMoveIntent`

Moves focus [delta] rows through the visible list.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

#### Constructor: `FluentTreeMoveIntent`

```dart
const FluentTreeMoveIntent(this.delta);
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `delta` | `int` | Yes | — | How many visible rows to move by. |

### `FluentTreeSelectionMode`

Whether rows carry a selection control, and how many may be chosen.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

```dart
enum FluentTreeSelectionMode {
  none,
  single,
  multiple,
}
```

### `FluentTreeSize`

Row height and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/navigation/tree.dart`

```dart
enum FluentTreeSize {
  small,
  medium,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentTreeItemState resolveFluentTreeItemState({
  bool enabled = true,
  int level = 1,
  bool branch = false,
  bool expanded = false,
  bool selected = false,
  FluentTreeSelectionMode selectionMode = FluentTreeSelectionMode.none,
  FluentTreeAppearance appearance = FluentTreeAppearance.subtle,
  FluentTreeSize size = FluentTreeSize.medium,
  Widget? icon,
  Widget? label,
  Widget? actions,
  ValueChanged<bool>? onSelectionChanged,
});

FluentTreeItemStyle resolveFluentTreeItemStyle(
  FluentTreeItemState state,
  FluentThemeData theme,
);

Widget buildFluentTreeItem(
  FluentTreeItemBaseState state,
  FluentTreeItemStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/tree_stories.dart`:

```dart
FluentTree(
    semanticLabel: 'Project files',
    size: knobs.get<FluentTreeSize>('size', FluentTreeSize.medium),
    appearance: knobs.get<FluentTreeAppearance>(
      'appearance',
      FluentTreeAppearance.subtle,
    ),
    defaultOpenItems: const <Object>{'lib'},
    items: _projectTree(
      icons: knobs.get<bool>('icons', true),
      disabledBranch: knobs.get<bool>('disabled', false),
    ),
  )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/navigation/tree.dart`
- Tests: `packages/fluent_2_web/test/goldens/tree_golden_test.dart`, `packages/fluent_2_web/test/navigation/tree_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/tree_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/tree/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
