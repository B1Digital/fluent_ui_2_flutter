# Carousel: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/carousel/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentCarousel`, `FluentCarouselStep`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentCarousel`

A Fluent 2 carousel: slides with previous/next chevrons, a step indicator and optional autoplay.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

#### Constructor: `FluentCarousel`

```dart
const FluentCarousel({
    super.key,
    required this.slides,
    this.previews,
    this.header,
    this.initialIndex = 0,
    this.onIndexChanged,
    this.enabled = true,
    this.loop = false,
    this.autoplay = false,
    this.autoplayInterval = fluentCarouselAutoplayInterval,
    this.layout = FluentCarouselLayout.outsideContent,
    this.chevronPlacement = FluentCarouselChevronPlacement.flexibleToEdges,
    this.pauseButton = FluentCarouselPauseButton.onContentClick,
    this.style,
    this.semanticLabel,
    this.previousLabel = 'Previous slide',
    this.nextLabel = 'Next slide',
    this.playLabel = 'Start automatic slide show',
    this.pauseLabel = 'Pause automatic slide show',
    this.stepLabel = defaultFluentCarouselStepLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `slides` | `List<Widget>` | Yes | — | The slides, in order. One page each. |
| `previews` | `List<Widget>?` | No | `null` | Thumbnails for the image-preview indicator, one per slide. |
| `header` | `Widget?` | No | `null` | Optional title/description block above the slides. |
| `initialIndex` | `int` | No | `0` | The slide shown on mount. |
| `onIndexChanged` | `ValueChanged<int>?` | No | `null` | Invoked whenever the current slide changes — by chevron, by step, by drag or by autoplay. |
| `enabled` | `bool` | No | `true` | Whether the carousel responds to input. False disables every control. |
| `loop` | `bool` | No | `false` | Whether the chevrons wrap around at the ends. |
| `autoplay` | `bool` | No | `false` | Whether the slides advance on their own. |
| `autoplayInterval` | `Duration` | No | `fluentCarouselAutoplayInterval` | How long each slide is shown. Upstream's `autoplayInterval`, 4000ms. |
| `layout` | `FluentCarouselLayout` | No | `FluentCarouselLayout.outsideContent` | Where the nav strip sits. |
| `chevronPlacement` | `FluentCarouselChevronPlacement` | No | `FluentCarouselChevronPlacement.flexibleToEdges` | Where the chevrons sit. |
| `pauseButton` | `FluentCarouselPauseButton` | No | `FluentCarouselPauseButton.onContentClick` | Where the autoplay affordance lives. |
| `style` | `FluentCarouselStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology for the carousel as a whole. |
| `previousLabel` | `String` | No | `'Previous slide'` | Announced for the previous-slide chevron. |
| `nextLabel` | `String` | No | `'Next slide'` | Announced for the next-slide chevron. |
| `playLabel` | `String` | No | `'Start automatic slide show'` | Announced for the autoplay button while paused. |
| `pauseLabel` | `String` | No | `'Pause automatic slide show'` | Announced for the autoplay button while playing. |
| `stepLabel` | `String Function(int index, int count)` | No | `defaultFluentCarouselStepLabel` | Announced for the step at a given index. |

#### State, callback, and accessibility fields

- `onIndexChanged` (`ValueChanged<int>?`): Invoked whenever the current slide changes — by chevron, by step, by drag or by autoplay.
- `enabled` (`bool`): Whether the carousel responds to input. False disables every control.
- `semanticLabel` (`String?`): Announced by assistive technology for the carousel as a whole.

### `FluentCarouselStep`

One indicator step: a dot, a pill when current, or an image thumbnail.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

#### Constructor: `FluentCarouselStep`

```dart
const FluentCarouselStep({
    super.key,
    required this.selected,
    required this.semanticLabel,
    this.onPressed,
    this.preview,
    this.style,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `selected` | `bool` | Yes | — | Whether this step's slide is the one in view. |
| `semanticLabel` | `String` | Yes | — | Announced by assistive technology. A step has no text of its own. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. Null disables the step. |
| `preview` | `Widget?` | No | `null` | The thumbnail for the image-preview indicator. Null draws a dot or pill. |
| `style` | `FluentCarouselStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |

#### State, callback, and accessibility fields

- `selected` (`bool`): Whether this step's slide is the one in view.
- `semanticLabel` (`String`): Announced by assistive technology. A step has no text of its own.
- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter. Null disables the step.

## Related public types

### `FluentCarouselBaseState`

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

#### Constructor: `FluentCarouselBaseState`

```dart
const FluentCarouselBaseState({
    required this.enabled,
    required this.count,
    required this.index,
    required this.viewport,
    required this.layout,
    required this.chevronPlacement,
    required this.previousLabel,
    required this.nextLabel,
    required this.stepLabel,
    this.header,
    this.previews,
    this.autoplayControl,
    this.onPrevious,
    this.onNext,
    this.onStepSelected,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the carousel responds to input at all. |
| `count` | `int` | Yes | — | How many slides there are. |
| `index` | `int` | Yes | — | The slide currently in view. |
| `viewport` | `Widget` | Yes | — | The already-built slide viewport. Behaviour — paging, autoplay, drag — lives in [FluentCarousel]; this is only what gets painted. |
| `layout` | `FluentCarouselLayout` | Yes | — | Where the nav strip sits. |
| `chevronPlacement` | `FluentCarouselChevronPlacement` | Yes | — | Where the chevrons sit. |
| `previousLabel` | `String` | Yes | — | Announced by assistive technology for the previous-slide chevron. |
| `nextLabel` | `String` | Yes | — | Announced by assistive technology for the next-slide chevron. |
| `stepLabel` | `String Function(int index, int count)` | Yes | — | Announced by assistive technology for the step at a given index. |
| `header` | `Widget?` | No | `null` | Optional title/description block above the slide. |
| `previews` | `List<Widget>?` | No | `null` | Thumbnails for the image-preview indicator, one per slide. Null renders the dot indicator instead. |
| `autoplayControl` | `Widget?` | No | `null` | The already-built play/pause button, or null when the carousel has no autoplay affordance in the nav. |
| `onPrevious` | `VoidCallback?` | No | `null` | Invoked by the previous chevron. Null disables it — which is a real state, not a paint job: at the first slide of a non-looping carousel it stops reporting hover, refuses focus, and never fires. |
| `onNext` | `VoidCallback?` | No | `null` | Invoked by the next chevron. Null disables it. |
| `onStepSelected` | `void Function(int index)?` | No | `null` | Invoked with the index of a tapped step. Null disables every step. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the carousel responds to input at all.
- `onPrevious` (`VoidCallback?`): Invoked by the previous chevron. Null disables it — which is a real state, not a paint job: at the first slide of a non-looping carousel it stops reporting hover, refuses focus, and never fires.
- `onNext` (`VoidCallback?`): Invoked by the next chevron. Null disables it.
- `onStepSelected` (`void Function(int index)?`): Invoked with the index of a tapped step. Null disables every step.

### `FluentCarouselChevronPlacement`

Where the previous/next chevrons sit. Figma's `Chevron placement` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

```dart
enum FluentCarouselChevronPlacement {
  flexibleToEdges,
  groupedToSteps,
  centeredToContent,
}
```

### `FluentCarouselLayout`

Where the nav strip sits relative to the slide. Figma's `Layout` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

```dart
enum FluentCarouselLayout {
  overContent,
  outsideContent,
}
```

### `FluentCarouselPauseButton`

Where the autoplay affordance lives. Figma's `Pause Button` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

```dart
enum FluentCarouselPauseButton {
  inNav,
  onContentClick,
}
```

### `FluentCarouselState`

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

#### Constructor: `FluentCarouselState`

```dart
const FluentCarouselState({
    required super.enabled,
    required super.count,
    required super.index,
    required super.viewport,
    required super.layout,
    required super.chevronPlacement,
    required super.previousLabel,
    required super.nextLabel,
    required super.stepLabel,
    required this.navType,
    super.header,
    super.previews,
    super.autoplayControl,
    super.onPrevious,
    super.onNext,
    super.onStepSelected,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the carousel responds to input at all. |
| `count` | `int` | Yes | — | How many slides there are. |
| `index` | `int` | Yes | — | The slide currently in view. |
| `viewport` | `Widget` | Yes | — | The already-built slide viewport. Behaviour — paging, autoplay, drag — lives in [FluentCarousel]; this is only what gets painted. |
| `layout` | `FluentCarouselLayout` | Yes | — | Where the nav strip sits. |
| `chevronPlacement` | `FluentCarouselChevronPlacement` | Yes | — | Where the chevrons sit. |
| `previousLabel` | `String` | Yes | — | Announced by assistive technology for the previous-slide chevron. |
| `nextLabel` | `String` | Yes | — | Announced by assistive technology for the next-slide chevron. |
| `stepLabel` | `String Function(int index, int count)` | Yes | — | Announced by assistive technology for the step at a given index. |
| `navType` | `FluentCarouselNavType` | Yes | — | Which indicator the strip shows. |
| `header` | `Widget?` | No | `null` | Optional title/description block above the slide. |
| `previews` | `List<Widget>?` | No | `null` | Thumbnails for the image-preview indicator, one per slide. Null renders the dot indicator instead. |
| `autoplayControl` | `Widget?` | No | `null` | The already-built play/pause button, or null when the carousel has no autoplay affordance in the nav. |
| `onPrevious` | `VoidCallback?` | No | `null` | Invoked by the previous chevron. Null disables it — which is a real state, not a paint job: at the first slide of a non-looping carousel it stops reporting hover, refuses focus, and never fires. |
| `onNext` | `VoidCallback?` | No | `null` | Invoked by the next chevron. Null disables it. |
| `onStepSelected` | `void Function(int index)?` | No | `null` | Invoked with the index of a tapped step. Null disables every step. |

### `FluentCarouselStyle`

Source: `packages/fluent_2_web/lib/src/surfaces/carousel_style.dart`

#### Constructor: `FluentCarouselStyle`

```dart
const FluentCarouselStyle({
    this.stepColor,
    this.stepBackgroundColor,
    this.stepBorderRadius,
    this.stepPadding,
    this.activeStepPadding,
    this.stepSize,
    this.activeStepSize,
    this.indicatorBackgroundColor,
    this.indicatorBorderRadius,
    this.previewSize,
    this.selectedPreviewSize,
    this.previewBorderRadius,
    this.previewGap,
    this.navGap,
    this.controlGap,
    this.navSpacing,
    this.navInset,
    this.iconSize,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `stepColor` | `WidgetStateProperty<Color?>?` | No | `null` | The dot or pill mark itself. Ramped rest/hover/pressed/disabled. |
| `stepBackgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | The step's 24x24 hit target. Fluent's transparent ramp, which is invisible in light and dark and **opaque** in high contrast. |
| `stepBorderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the step's hit target. |
| `stepPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around an inactive step's dot. |
| `activeStepPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the active step's pill. Narrower horizontally, because the pill is wider and the hit target stays square. |
| `stepSize` | `WidgetStateProperty<Size?>?` | No | `null` | Size of an inactive step's dot. |
| `activeStepSize` | `WidgetStateProperty<Size?>?` | No | `null` | Size of the active step's pill. |
| `indicatorBackgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Fill behind the whole indicator strip. Null in the `outsideContent` layout; a translucent wash when the strip sits over the slide. |
| `indicatorBorderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the indicator strip. |
| `previewSize` | `WidgetStateProperty<Size?>?` | No | `null` | Size of an unselected image-preview thumbnail. |
| `selectedPreviewSize` | `WidgetStateProperty<Size?>?` | No | `null` | Size of the selected image-preview thumbnail. |
| `previewBorderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of an image-preview thumbnail. |
| `previewGap` | `WidgetStateProperty<double?>?` | No | `null` | Gap between image-preview thumbnails. |
| `navGap` | `WidgetStateProperty<double?>?` | No | `null` | Gap between the chevron groups and the indicator strip. |
| `controlGap` | `WidgetStateProperty<double?>?` | No | `null` | Gap between the autoplay control and the previous-slide chevron. |
| `navSpacing` | `WidgetStateProperty<double?>?` | No | `null` | Gap between the slide and the nav strip. |
| `navInset` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset from the carousel's edges to the outermost chevron. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Edge length of the chevron and autoplay glyphs. |

#### State, callback, and accessibility fields

- `selectedPreviewSize` (`WidgetStateProperty<Size?>?`): Size of the selected image-preview thumbnail.

### `FluentCarouselTheme`

Overrides the carousel style for a subtree.

Source: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`

#### Constructor: `FluentCarouselTheme`

```dart
const FluentCarouselTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentCarouselStyle` | Yes | — | The style layered over the nav-type defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentCarouselState resolveFluentCarouselState({
  Widget viewport = const SizedBox.shrink(),
  bool enabled = true,
  int count = 0,
  int index = 0,
  FluentCarouselLayout layout = FluentCarouselLayout.outsideContent,
  FluentCarouselChevronPlacement chevronPlacement =
      FluentCarouselChevronPlacement.flexibleToEdges,
  FluentCarouselNavType navType = FluentCarouselNavType.steps,
  String previousLabel = 'Previous slide',
  String nextLabel = 'Next slide',
  String Function(int index, int count) stepLabel =
      defaultFluentCarouselStepLabel,
  Widget? header,
  List<Widget>? previews,
  Widget? autoplayControl,
  VoidCallback? onPrevious,
  VoidCallback? onNext,
  void Function(int index)? onStepSelected,
});

FluentCarouselStyle resolveFluentCarouselStyle(
  FluentCarouselState state,
  FluentThemeData theme,
);

Widget buildFluentCarousel(
  FluentCarouselBaseState state,
  FluentCarouselStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/carousel_stories.dart`:

```dart
FluentCarousel(
            slides: _slides(5),
            layout: knobs.get<FluentCarouselLayout>(
              'layout',
              FluentCarouselLayout.outsideContent,
            ),
            chevronPlacement: knobs.get<FluentCarouselChevronPlacement>(
              'placement',
              FluentCarouselChevronPlacement.flexibleToEdges,
            ),
            loop: knobs.get<bool>('loop', false),
            autoplay: knobs.get<bool>('autoplay', false),
            enabled: knobs.get<bool>('enabled', true),
            semanticLabel: 'Product highlights',
          )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/surfaces/carousel.dart`
- Tests: `packages/fluent_2_web/test/goldens/carousel_golden_test.dart`, `packages/fluent_2_web/test/surfaces/carousel_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/carousel_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/carousel/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
