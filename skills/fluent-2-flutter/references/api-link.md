# Link: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/link/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentLink`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentLink`

A Fluent 2 link.

Source: `packages/fluent_2/lib/src/inputs/link.dart`

#### Constructor: `FluentLink`

```dart
const FluentLink({
    super.key,
    required this.child,
    this.onPressed,
    this.appearance = FluentLinkAppearance.standard,
    this.inline = false,
    this.icon,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The link text. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. Null disables the link. |
| `appearance` | `FluentLinkAppearance` | No | `FluentLinkAppearance.standard` | Colour treatment. |
| `inline` | `bool` | No | `false` | Whether to underline at rest, for a link sitting inside prose. |
| `icon` | `Widget?` | No | `null` | Optional trailing icon. |
| `style` | `FluentLinkStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology **in place of** the label text. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter. Null disables the link.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology **in place of** the label text.

## Related public types

### `FluentLinkAppearance`

How a link is coloured.

Source: `packages/fluent_2/lib/src/inputs/link.dart`

```dart
enum FluentLinkAppearance {
  standard,
  subtle,
  overBrand,
}
```

### `FluentLinkBaseState`

Source: `packages/fluent_2/lib/src/inputs/link.dart`

#### Constructor: `FluentLinkBaseState`

```dart
const FluentLinkBaseState({
    required this.enabled,
    required this.label,
    this.icon,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the link responds to input. |
| `label` | `Widget` | Yes | — | The link text. Always present — Figma's `Text#41437:0` property is not optional, and a link with no text has nothing to activate. |
| `icon` | `Widget?` | No | `null` | Optional trailing icon. Figma's `Show Icon#23193:0` boolean, which places it after the label in reading order. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the link responds to input.

### `FluentLinkState`

Source: `packages/fluent_2/lib/src/inputs/link.dart`

#### Constructor: `FluentLinkState`

```dart
const FluentLinkState({
    required super.enabled,
    required super.label,
    required this.appearance,
    required this.inline,
    super.icon,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the link responds to input. |
| `label` | `Widget` | Yes | — | The link text. Always present — Figma's `Text#41437:0` property is not optional, and a link with no text has nothing to activate. |
| `appearance` | `FluentLinkAppearance` | Yes | — | Colour treatment. |
| `inline` | `bool` | Yes | — | Whether the link is underlined at rest. |
| `icon` | `Widget?` | No | `null` | Optional trailing icon. Figma's `Show Icon#23193:0` boolean, which places it after the label in reading order. |

### `FluentLinkStyle`

Source: `packages/fluent_2/lib/src/inputs/link_style.dart`

#### Constructor: `FluentLinkStyle`

```dart
const FluentLinkStyle({
    this.foregroundColor,
    this.decoration,
    this.decorationColor,
    this.decorationStyle,
    this.decorationThickness,
    this.textStyle,
    this.padding,
    this.gap,
    this.iconSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label and icon colour. |
| `decoration` | `WidgetStateProperty<TextDecoration?>?` | No | `null` | Whether the label is underlined. |
| `decorationColor` | `WidgetStateProperty<Color?>?` | No | `null` | Underline colour. Tracks [foregroundColor] in every state except focus, where Figma recolours it to the focus stroke. |
| `decorationStyle` | `WidgetStateProperty<TextDecorationStyle?>?` | No | `null` | Underline style. Figma draws two lines under a focused link and one everywhere else, which is `TextDecorationStyle.double` and `TextDecorationStyle.solid` respectively. |
| `decorationThickness` | `WidgetStateProperty<double?>?` | No | `null` | Underline width, in logical pixels. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour and decoration are overridden by [foregroundColor] and the `decoration*` properties. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding around the label and icon. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between label and icon. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Icon edge length. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentLinkTheme`

Overrides the link style for a subtree.

Source: `packages/fluent_2/lib/src/inputs/link.dart`

#### Constructor: `FluentLinkTheme`

```dart
const FluentLinkTheme({super.key, required this.style, required super.child});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentLinkStyle` | Yes | — | The style layered over the appearance defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentLinkState resolveFluentLinkState({
  required Widget label,
  bool enabled = true,
  FluentLinkAppearance appearance = FluentLinkAppearance.standard,
  bool inline = false,
  Widget? icon,
});

FluentLinkStyle resolveFluentLinkStyle(
  FluentLinkState state,
  FluentThemeData theme,
);

Widget buildFluentLink(
  FluentLinkBaseState state,
  FluentLinkStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`:

```dart
FluentLink(onPressed: () {}, child: const Text('This is a link'))
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/inputs/link.dart`
- Tests: `packages/fluent_2/test/goldens/link_golden_test.dart`, `packages/fluent_2/test/goldens/toast_golden_test.dart`, `packages/fluent_2/test/inputs/link_test.dart`, `packages/fluent_2/test/navigation/data_grid_test.dart`, `packages/fluent_2/test/overlays/toast_test.dart`
- Stories: `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/link/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
