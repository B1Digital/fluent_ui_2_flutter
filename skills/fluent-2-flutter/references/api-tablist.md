# Tab list: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/tablist/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentTabList`, `FluentTab`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentTabList<T extends Object>`

A Fluent 2 tab list.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabList`

```dart
const FluentTabList({
    super.key,
    required this.tabs,
    this.selectedValue,
    this.onSelect,
    this.orientation = FluentTabOrientation.horizontal,
    this.size = FluentTabSize.medium,
    this.appearance = FluentTabAppearance.transparent,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `tabs` | `List<FluentTab<T>>` | Yes | — | The tabs, in reading order. |
| `selectedValue` | `T?` | No | `null` | The selected tab's value, or null when nothing is selected. |
| `onSelect` | `ValueChanged<T>?` | No | `null` | Invoked with the newly selected value. Null disables every tab. |
| `orientation` | `FluentTabOrientation` | No | `FluentTabOrientation.horizontal` | Which way the list runs. |
| `size` | `FluentTabSize` | No | `FluentTabSize.medium` | Height and type ramp for every tab. |
| `appearance` | `FluentTabAppearance` | No | `FluentTabAppearance.transparent` | Fill and shape treatment for every tab. |
| `style` | `FluentTabStyle?` | No | `null` | Overrides layered over the theme defaults for every tab. A tab's own `style` is merged after this one. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the name of the tab list. |

#### State, callback, and accessibility fields

- `selectedValue` (`T?`): The selected tab's value, or null when nothing is selected.
- `onSelect` (`ValueChanged<T>?`): Invoked with the newly selected value. Null disables every tab.
- `semanticLabel` (`String?`): Announced by assistive technology as the name of the tab list.

### `FluentTab<T extends Object>`

One tab of a [FluentTabList].

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTab`

```dart
const FluentTab({
    super.key,
    required this.value,
    this.child,
    this.icon,
    this.enabled = true,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `T` | Yes | — | Identifies this tab within its list. Compared with `==`. |
| `child` | `Widget?` | No | `null` | The label. Null for an icon-only tab, which should then carry a [semanticLabel]. |
| `icon` | `Widget?` | No | `null` | Optional leading icon. |
| `enabled` | `bool` | No | `true` | Whether the tab can be selected. |
| `style` | `FluentTabStyle?` | No | `null` | Overrides layered over the list's style. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology. Required in practice for an icon-only tab, which has no text to announce. |

#### State, callback, and accessibility fields

- `value` (`T`): Identifies this tab within its list. Compared with `==`.
- `enabled` (`bool`): Whether the tab can be selected.
- `semanticLabel` (`String?`): Announced by assistive technology. Required in practice for an icon-only tab, which has no text to announce.

## Related public types

### `FluentTabAppearance`

Fill and shape treatment. Figma's `Style` axis.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

```dart
enum FluentTabAppearance {
  transparent,
  subtle,
  filledCircular,
  subtleCircular,
}
```

### `FluentTabBaseState`

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabBaseState`

```dart
const FluentTabBaseState({
    required this.enabled,
    required this.selected,
    required this.orientation,
    this.icon,
    this.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the tab responds to input. |
| `selected` | `bool` | Yes | — | Whether this is the selected tab of its list. |
| `orientation` | `FluentTabOrientation` | Yes | — | Which edge the selection indicator runs along. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A tab with no label is an icon-only tab. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the tab responds to input.
- `selected` (`bool`): Whether this is the selected tab of its list.

### `FluentTabEdgeIntent`

Moves the selection to the first or last enabled tab.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabEdgeIntent`

```dart
const FluentTabEdgeIntent({required this.last});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `last` | `bool` | Yes | — | Whether to jump to the last tab rather than the first. |

### `FluentTabMoveIntent`

Moves the selection by [delta] tabs, skipping disabled ones.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabMoveIntent`

```dart
const FluentTabMoveIntent(this.delta);
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `delta` | `int` | Yes | — | How many tabs to move by. Negative moves towards the first tab. |

### `FluentTabOrientation`

Which way a tab list runs.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

```dart
enum FluentTabOrientation {
  horizontal,
  vertical,
}
```

### `FluentTabSize`

Tab height and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

```dart
enum FluentTabSize {
  medium,
  small,
}
```

### `FluentTabState`

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabState`

```dart
const FluentTabState({
    required super.enabled,
    required super.selected,
    required super.orientation,
    required this.size,
    required this.appearance,
    super.icon,
    super.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the tab responds to input. |
| `selected` | `bool` | Yes | — | Whether this is the selected tab of its list. |
| `orientation` | `FluentTabOrientation` | Yes | — | Which edge the selection indicator runs along. |
| `size` | `FluentTabSize` | Yes | — | Height and type ramp. |
| `appearance` | `FluentTabAppearance` | Yes | — | Fill and shape treatment. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A tab with no label is an icon-only tab. |

### `FluentTabStyle`

Source: `packages/fluent_2_web/lib/src/navigation/tab_style.dart`

#### Constructor: `FluentTabStyle`

```dart
const FluentTabStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.iconColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.focusRingRadius,
    this.textStyle,
    this.padding,
    this.gap,
    this.iconSize,
    this.minimumSize,
    this.indicatorColor,
    this.indicatorThickness,
    this.indicatorInset,
    this.indicatorRadius,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label colour. |
| `iconColor` | `WidgetStateProperty<Color?>?` | No | `null` | Icon colour. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the tab surface. |
| `focusRingRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the keyboard focus ring. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [foregroundColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the border. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between icon and label. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Icon edge length. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum tap target. |
| `indicatorColor` | `WidgetStateProperty<Color?>?` | No | `null` | Selection indicator colour, or null for no indicator. |
| `indicatorThickness` | `WidgetStateProperty<double?>?` | No | `null` | Selection indicator thickness. Zero suppresses it. |
| `indicatorInset` | `WidgetStateProperty<double?>?` | No | `null` | How far the indicator is held back from each end of the tab. |
| `indicatorRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Selection indicator corner radius. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentTabTheme`

Overrides the tab style for a subtree.

Source: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`

#### Constructor: `FluentTabTheme`

```dart
const FluentTabTheme({super.key, required this.style, required super.child});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentTabStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentTabState resolveFluentTabState({
  bool enabled = true,
  bool selected = false,
  FluentTabOrientation orientation = FluentTabOrientation.horizontal,
  FluentTabSize size = FluentTabSize.medium,
  FluentTabAppearance appearance = FluentTabAppearance.transparent,
  Widget? icon,
  Widget? label,
});

FluentTabStyle resolveFluentTabStyle(
  FluentTabState state,
  FluentThemeData theme,
);

Widget buildFluentTab(
  FluentTabBaseState state,
  FluentTabStyle style,
  Set<WidgetState> states, {
  Animation<double>? indicatorFlip,
  double indicatorTranslation = 0,
  double indicatorScale = 1,
});
```

## Verified usage

No dedicated checked-in constructor excerpt is available. Use the exact
signatures above and verify any new example with Dart analysis and a widget test.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/navigation/tab_list.dart`
- Tests: `packages/fluent_2_web/test/goldens/tab_list_golden_test.dart`, `packages/fluent_2_web/test/navigation/tab_list_test.dart`
- Stories: `packages/fluent_2_web/example/lib/storybook/components/navigation_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/tablist/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
