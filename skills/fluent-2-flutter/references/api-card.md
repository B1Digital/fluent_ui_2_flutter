# Card: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/card/usage/

Design decisions and best practices: [components-surfaces-feedback.md](./components-surfaces-feedback.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentCard`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentCard`

A Fluent 2 card: a container that groups a preview, a header, a body and a footer onto one elevated surface.

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

#### Constructor: `FluentCard`

```dart
const FluentCard({
    super.key,
    this.child,
    this.preview,
    this.header,
    this.footer,
    this.onPressed,
    this.selected = false,
    this.disabled = false,
    this.appearance = FluentCardAppearance.filled,
    this.orientation = FluentCardOrientation.vertical,
    this.size = FluentCardSize.medium,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget?` | No | `null` | The body slot, inset by the card's padding. |
| `preview` | `Widget?` | No | `null` | Media that bleeds to the card's edge, before every other slot. |
| `header` | `Widget?` | No | `null` | The header slot, inset by the card's padding. |
| `footer` | `Widget?` | No | `null` | The footer slot, inset by the card's padding. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. |
| `selected` | `bool` | No | `false` | Whether the card is selected, which selects Fluent's real `*Selected` tokens and outranks hover, as it does upstream. |
| `disabled` | `bool` | No | `false` | Whether the card is disabled. |
| `appearance` | `FluentCardAppearance` | No | `FluentCardAppearance.filled` | Fill, outline and elevation treatment. |
| `orientation` | `FluentCardOrientation` | No | `FluentCardOrientation.vertical` | Which axis the slots are laid out along. |
| `size` | `FluentCardSize` | No | `FluentCardSize.medium` | Inset, gap and corner radius. |
| `style` | `FluentCardStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. Ignored by an inert card, which takes no focus. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter.
- `selected` (`bool`): Whether the card is selected, which selects Fluent's real `*Selected` tokens and outranks hover, as it does upstream.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted. Ignored by an inert card, which takes no focus.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology.

## Related public types

### `FluentCardAppearance`

How a card is filled and outlined.

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

```dart
enum FluentCardAppearance {
  filled,
  filledAlternative,
  outline,
  subtle,
}
```

### `FluentCardBaseState`

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

#### Constructor: `FluentCardBaseState`

```dart
const FluentCardBaseState({
    required this.enabled,
    required this.interactive,
    required this.selected,
    required this.orientation,
    this.preview,
    this.header,
    this.body,
    this.footer,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the card responds to input at all. |
| `interactive` | `bool` | Yes | — | Whether the card was given an `onPressed`. |
| `selected` | `bool` | Yes | — | Whether the card is currently selected. |
| `orientation` | `FluentCardOrientation` | Yes | — | Which axis the slots are laid out along. |
| `preview` | `Widget?` | No | `null` | Media that bleeds to the card's edge, before every other slot. |
| `header` | `Widget?` | No | `null` | Title area, inset by the card's padding. |
| `body` | `Widget?` | No | `null` | Main content, inset by the card's padding. |
| `footer` | `Widget?` | No | `null` | Action area, inset by the card's padding. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the card responds to input at all.
- `selected` (`bool`): Whether the card is currently selected.

### `FluentCardOrientation`

Which axis the slots are laid out along. Figma's `Layout` axis.

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

```dart
enum FluentCardOrientation {
  vertical,
  horizontal,
}
```

### `FluentCardSize`

Card inset, gap and corner radius. Figma's `Card padding` collection.

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

```dart
enum FluentCardSize {
  small,
  medium,
  large,
}
```

### `FluentCardState`

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

#### Constructor: `FluentCardState`

```dart
const FluentCardState({
    required super.enabled,
    required super.interactive,
    required super.selected,
    required super.orientation,
    required this.appearance,
    required this.size,
    super.preview,
    super.header,
    super.body,
    super.footer,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the card responds to input at all. |
| `interactive` | `bool` | Yes | — | Whether the card was given an `onPressed`. |
| `selected` | `bool` | Yes | — | Whether the card is currently selected. |
| `orientation` | `FluentCardOrientation` | Yes | — | Which axis the slots are laid out along. |
| `appearance` | `FluentCardAppearance` | Yes | — | Fill, outline and elevation treatment. |
| `size` | `FluentCardSize` | Yes | — | Inset, gap and corner radius. |
| `preview` | `Widget?` | No | `null` | Media that bleeds to the card's edge, before every other slot. |
| `header` | `Widget?` | No | `null` | Title area, inset by the card's padding. |
| `body` | `Widget?` | No | `null` | Main content, inset by the card's padding. |
| `footer` | `Widget?` | No | `null` | Action area, inset by the card's padding. |

### `FluentCardStyle`

Source: `packages/fluent_2/lib/src/surfaces/card_style.dart`

#### Constructor: `FluentCardStyle`

```dart
const FluentCardStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.padding,
    this.gap,
    this.shadow,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour inherited by the slots' text and icons. Upstream sets only the colour on the card root — never a font — so neither does this. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius. The surface, the border and the clip all share it. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around every slot except the preview, which bleeds to the edge. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between slots. Upstream drives padding and gap from one CSS variable, so they are equal at every size — but they are separate properties here so either can be overridden alone. |
| `shadow` | `WidgetStateProperty<List<BoxShadow>?>?` | No | `null` | Elevation shadow cast by the surface. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. Only consulted on an interactive card. |

### `FluentCardTheme`

Overrides the card style for a subtree.

Source: `packages/fluent_2/lib/src/surfaces/card.dart`

#### Constructor: `FluentCardTheme`

```dart
const FluentCardTheme({super.key, required this.style, required super.child});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentCardStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentCardState resolveFluentCardState({
  bool enabled = true,
  bool interactive = false,
  bool selected = false,
  FluentCardAppearance appearance = FluentCardAppearance.filled,
  FluentCardOrientation orientation = FluentCardOrientation.vertical,
  FluentCardSize size = FluentCardSize.medium,
  Widget? preview,
  Widget? header,
  Widget? body,
  Widget? footer,
});

FluentCardStyle resolveFluentCardStyle(
  FluentCardState state,
  FluentThemeData theme,
);

Widget buildFluentCard(
  FluentCardBaseState state,
  FluentCardStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/storybook/components/surfaces_stories.dart`:

```dart
FluentCard(
            header: Text('Card title'),
            preview: SizedBox(height: 120, width: 240),
            footer: Text('Footer'),
            child: Text('Body content goes here.'),
          )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/surfaces/card.dart`
- Tests: `packages/fluent_2/test/goldens/card_golden_test.dart`, `packages/fluent_2/test/surfaces/card_test.dart`
- Stories: `packages/fluent_2/example/lib/storybook/components/surfaces_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/card/usage/
- Design decisions: `references/components-surfaces-feedback.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
