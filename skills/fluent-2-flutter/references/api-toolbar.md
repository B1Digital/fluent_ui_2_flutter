# Toolbar: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/toolbar/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentToolbar`, `FluentToolbarDivider`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentToolbar`

A Fluent 2 toolbar: a row of buttons, toggles and dividers on one surface.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbar`

```dart
const FluentToolbar({
    super.key,
    required this.items,
    this.size = FluentToolbarSize.medium,
    this.type = FluentToolbarType.standard,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `items` | `List<Widget>` | Yes | — | The row's children, in order. Dividers are ordinary members. |
| `size` | `FluentToolbarSize` | No | `FluentToolbarSize.medium` | How far the surface is inset from its content. |
| `type` | `FluentToolbarType` | No | `FluentToolbarType.standard` | Whether the surface is elevated. |
| `style` | `FluentToolbarStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the name of the group. |

#### State, callback, and accessibility fields

- `semanticLabel` (`String?`): Announced by assistive technology as the name of the group.

### `FluentToolbarDivider`

The vertical rule that separates groups of toolbar items.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbarDivider`

```dart
const FluentToolbarDivider({super.key, this.style});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentToolbarStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |

## Related public types

### `FluentToolbarBaseState`

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbarBaseState`

```dart
const FluentToolbarBaseState({required this.items});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `items` | `List<Widget>` | Yes | — | The row's children, in order, already wrapped for roving focus by [FluentToolbar]. Dividers are ordinary members of this list. |

### `FluentToolbarMoveIntent`

Moves the roving focus by [FluentToolbarMoveIntent.delta] items.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbarMoveIntent`

```dart
const FluentToolbarMoveIntent(this.delta);
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `delta` | `int` | Yes | — | How many items to move. Negative walks towards the start of the row. In an RTL subtree the horizontal arrows are swapped before this is dispatched, so `1` always means "towards the end of the item list". |

### `FluentToolbarSize`

Toolbar padding ramp. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

```dart
enum FluentToolbarSize {
  small,
  medium,
  large,
}
```

### `FluentToolbarState`

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbarState`

```dart
const FluentToolbarState({
    required super.items,
    required this.size,
    required this.type,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `items` | `List<Widget>` | Yes | — | The row's children, in order, already wrapped for roving focus by [FluentToolbar]. Dividers are ordinary members of this list. |
| `size` | `FluentToolbarSize` | Yes | — | How far the surface is inset from its content. |
| `type` | `FluentToolbarType` | Yes | — | Whether the surface is elevated. |

### `FluentToolbarStyle`

Source: `packages/fluent_2_web/lib/src/navigation/toolbar_style.dart`

#### Constructor: `FluentToolbarStyle`

```dart
const FluentToolbarStyle({
    this.backgroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.padding,
    this.gap,
    this.shadow,
    this.dividerWidth,
    this.dividerPadding,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast, which is the only thing that outlines a toolbar there. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the border. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between items. Zero in every Figma variant; toolbar items are self-padding and butt against each other. |
| `shadow` | `WidgetStateProperty<List<BoxShadow>?>?` | No | `null` | Elevation cast by the surface, or null for a flat one. |
| `dividerWidth` | `WidgetStateProperty<double?>?` | No | `null` | Total width of a `FluentToolbarDivider`'s slot, hairline included. |
| `dividerPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset between the divider's rule and the top and bottom of the item row. |

### `FluentToolbarTheme`

Overrides the toolbar style for a subtree.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

#### Constructor: `FluentToolbarTheme`

```dart
const FluentToolbarTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentToolbarStyle` | Yes | — | The style layered over the size and type defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentToolbarType`

Whether the toolbar sits in the page or floats over it. Figma's `Type` axis.

Source: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`

```dart
enum FluentToolbarType {
  standard,
  floating,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentToolbarState resolveFluentToolbarState({
  required List<Widget> items,
  FluentToolbarSize size = FluentToolbarSize.medium,
  FluentToolbarType type = FluentToolbarType.standard,
});

FluentToolbarStyle resolveFluentToolbarStyle(
  FluentToolbarState state,
  FluentThemeData theme,
);

Widget buildFluentToolbar(
  FluentToolbarBaseState state,
  FluentToolbarStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/toolbar_stories.dart`:

```dart
FluentToolbar(
    semanticLabel: 'Formatting',
    size: knobs.get<FluentToolbarSize>('size', FluentToolbarSize.medium),
    type: knobs.get<FluentToolbarType>('type', FluentToolbarType.standard),
    items: <Widget>[
      ..._formatting,
      if (dividers) const FluentToolbarDivider(),
      _item(FluentIcons.text_bullet_list_20_regular, 'Bulleted list'),
      _item(FluentIcons.text_number_list_ltr_20_regular, 'Numbered list'),
      if (dividers) const FluentToolbarDivider(),
      _item(FluentIcons.link_20_regular, 'Insert link'),
      _item(FluentIcons.image_20_regular, 'Insert image'),
    ],
  )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/navigation/toolbar.dart`
- Tests: `packages/fluent_2_web/test/goldens/toolbar_golden_test.dart`, `packages/fluent_2_web/test/navigation/toolbar_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/toolbar_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/toolbar/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
