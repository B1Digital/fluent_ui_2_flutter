# Spinner: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/spinner/usage/

Design decisions and best practices: [components-surfaces-feedback.md](./components-surfaces-feedback.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentSpinner`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentSpinner`

A Fluent 2 spinner: an indeterminate progress indicator.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

#### Constructor: `FluentSpinner`

```dart
const FluentSpinner({
    super.key,
    this.label,
    this.appearance = FluentSpinnerAppearance.primary,
    this.size = FluentSpinnerSize.medium,
    this.labelPosition = FluentSpinnerLabelPosition.after,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `label` | `Widget?` | No | `null` | The visible label. Null for a bare ring. |
| `appearance` | `FluentSpinnerAppearance` | No | `FluentSpinnerAppearance.primary` | Ring and label colouring. |
| `size` | `FluentSpinnerSize` | No | `FluentSpinnerSize.medium` | Diameter, thickness and type ramp. |
| `labelPosition` | `FluentSpinnerLabelPosition` | No | `FluentSpinnerLabelPosition.after` | Where [label] sits relative to the ring. |
| `style` | `FluentSpinnerStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology. |

#### State, callback, and accessibility fields

- `semanticLabel` (`String?`): Announced by assistive technology.

## Related public types

### `FluentSpinnerAppearance`

How a spinner's ring is coloured. Figma's `Style` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

```dart
enum FluentSpinnerAppearance {
  primary,
  subtle,
}
```

### `FluentSpinnerBaseState`

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

#### Constructor: `FluentSpinnerBaseState`

```dart
const FluentSpinnerBaseState({required this.labelPosition, this.label});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `labelPosition` | `FluentSpinnerLabelPosition` | Yes | — | Where the label sits relative to the ring. |
| `label` | `Widget?` | No | `null` | The label, if any. A spinner with no label is a bare ring. |

### `FluentSpinnerLabelPosition`

Where the label sits relative to the ring. Figma's `Layout` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

```dart
enum FluentSpinnerLabelPosition {
  after,
  before,
  above,
  below,
}
```

### `FluentSpinnerPainter`

Paints the spinner's track and tail.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

#### Constructor: `FluentSpinnerPainter`

```dart
const FluentSpinnerPainter({
    required this.trackColor,
    required this.indicatorColor,
    required this.strokeWidth,
    required this.pose,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `trackColor` | `Color` | Yes | — | The full-circle rail. |
| `indicatorColor` | `Color` | Yes | — | The moving arc drawn over the rail. |
| `strokeWidth` | `double` | Yes | — | Thickness of both, in logical pixels. |
| `pose` | `FluentSpinnerPose` | Yes | — | Where the tail currently is. |

### `FluentSpinnerSize`

Ring diameter, thickness and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

```dart
enum FluentSpinnerSize {
  extraTiny,
  tiny,
  extraSmall,
  small,
  medium,
  large,
  extraLarge,
  huge,
}
```

### `FluentSpinnerState`

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

#### Constructor: `FluentSpinnerState`

```dart
const FluentSpinnerState({
    required super.labelPosition,
    required this.appearance,
    required this.size,
    super.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `labelPosition` | `FluentSpinnerLabelPosition` | Yes | — | Where the label sits relative to the ring. |
| `appearance` | `FluentSpinnerAppearance` | Yes | — | Ring and label colouring. |
| `size` | `FluentSpinnerSize` | Yes | — | Diameter, thickness and type ramp. |
| `label` | `Widget?` | No | `null` | The label, if any. A spinner with no label is a bare ring. |

### `FluentSpinnerStyle`

Source: `packages/fluent_2_web/lib/src/surfaces/spinner_style.dart`

#### Constructor: `FluentSpinnerStyle`

```dart
const FluentSpinnerStyle({
    this.trackColor,
    this.indicatorColor,
    this.labelColor,
    this.strokeWidth,
    this.diameter,
    this.textStyle,
    this.gap,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `trackColor` | `WidgetStateProperty<Color?>?` | No | `null` | The full-circle rail the tail travels along. Figma calls this layer `Track`. |
| `indicatorColor` | `WidgetStateProperty<Color?>?` | No | `null` | The moving arc drawn over the track. Figma calls this layer `Tail`. |
| `labelColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label colour. Overrides the colour carried by [textStyle]. |
| `strokeWidth` | `WidgetStateProperty<double?>?` | No | `null` | Ring thickness, for both the track and the tail. |
| `diameter` | `WidgetStateProperty<double?>?` | No | `null` | Outer edge length of the ring's square box. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [labelColor]. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the ring and the label, on whichever axis the label sits. |

### `FluentSpinnerTheme`

Overrides the spinner style for a subtree.

Source: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`

#### Constructor: `FluentSpinnerTheme`

```dart
const FluentSpinnerTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentSpinnerStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentSpinnerState resolveFluentSpinnerState({
  FluentSpinnerAppearance appearance = FluentSpinnerAppearance.primary,
  FluentSpinnerSize size = FluentSpinnerSize.medium,
  FluentSpinnerLabelPosition labelPosition = FluentSpinnerLabelPosition.after,
  Widget? label,
});

FluentSpinnerStyle resolveFluentSpinnerStyle(
  FluentSpinnerState state,
  FluentThemeData theme,
);

Widget buildFluentSpinner(
  FluentSpinnerBaseState state,
  FluentSpinnerStyle style,
  Set<WidgetState> states, {
  FluentSpinnerPose pose = FluentSpinnerPose.resting,
});
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/button_stories.dart`:

```dart
FluentSpinner(
            size: FluentSpinnerSize.tiny,
            semanticLabel: 'Uploading',
          )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/surfaces/spinner.dart`
- Tests: `packages/fluent_2_web/test/goldens/spinner_golden_test.dart`, `packages/fluent_2_web/test/surfaces/spinner_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/button_stories.dart`, `packages/fluent_2_web/example/lib/stories/compound_button_stories.dart`, `packages/fluent_2_web/example/lib/stories/spinner_stories.dart`, `packages/fluent_2_web/example/lib/stories/toast_stories.dart`, `packages/fluent_2_web/example/lib/stories/tree_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/spinner/usage/
- Design decisions: `references/components-surfaces-feedback.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
