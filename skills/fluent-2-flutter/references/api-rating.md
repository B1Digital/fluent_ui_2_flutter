# Rating: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/rating/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentRating`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentRating`

A Fluent 2 rating.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

#### Constructor: `FluentRating`

```dart
const FluentRating({
    super.key,
    required this.value,
    this.onChanged,
    this.max = 5,
    this.step = 1,
    this.compact = false,
    this.type = FluentRatingType.interactive,
    this.size = FluentRatingSize.extraLarge,
    this.shape = FluentRatingShape.star,
    this.color = FluentRatingColor.neutral,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `double` | Yes | — | The rating being shown. Rounded to the nearest half before it is drawn. |
| `onChanged` | `ValueChanged<double>?` | No | `null` | Invoked with the new value on click and on the arrow keys. Null disables the rating. |
| `max` | `int` | No | `5` | How many shapes a full rating is worth. |
| `step` | `double` | No | `1` | The granularity the control commits at: 1, or 0.5 for half values. |
| `compact` | `bool` | No | `false` | Whether a single filled shape stands in for the whole row. |
| `type` | `FluentRatingType` | No | `FluentRatingType.interactive` | Whether the rating takes input. |
| `size` | `FluentRatingSize` | No | `FluentRatingSize.extraLarge` | Shape box edge length. |
| `shape` | `FluentRatingShape` | No | `FluentRatingShape.star` | Which silhouette is drawn. |
| `color` | `FluentRatingColor` | No | `FluentRatingColor.neutral` | Which colour family the shapes take. |
| `style` | `FluentRatingStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology alongside the value. |

#### State, callback, and accessibility fields

- `value` (`double`): The rating being shown. Rounded to the nearest half before it is drawn.
- `onChanged` (`ValueChanged<double>?`): Invoked with the new value on click and on the arrow keys. Null disables the rating.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology alongside the value.

## Related public types

### `FluentRatingBaseState`

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

#### Constructor: `FluentRatingBaseState`

```dart
const FluentRatingBaseState({
    required this.value,
    required this.max,
    required this.compact,
    required this.enabled,
    this.step = 1,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `value` | `double` | Yes | — | The rating being shown, rounded to the nearest half. |
| `max` | `int` | Yes | — | How many shapes a full rating is worth. |
| `compact` | `bool` | Yes | — | Whether a single filled shape stands in for the whole row. |
| `enabled` | `bool` | Yes | — | Whether the rating responds to input. |
| `step` | `double` | No | `1` | The granularity the control commits at: 1, or 0.5 for half values. |

#### State, callback, and accessibility fields

- `value` (`double`): The rating being shown, rounded to the nearest half.
- `enabled` (`bool`): Whether the rating responds to input.

### `FluentRatingColor`

Which colour family the shapes take. Figma's `Rating color` collection.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

```dart
enum FluentRatingColor {
  neutral,
  brand,
  marigold,
}
```

### `FluentRatingPainter`

Paints a row of rating shapes, each filled by its own fraction.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

#### Constructor: `FluentRatingPainter`

```dart
const FluentRatingPainter({
    required this.shape,
    required this.itemSize,
    required this.gap,
    required this.fills,
    required this.selected,
    required this.unselected,
    required this.unselectedStroke,
    required this.strokeWidth,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `shape` | `FluentRatingShape` | Yes | — | Which silhouette every shape in the row takes. |
| `itemSize` | `double` | Yes | — | Edge length of one shape's box. |
| `gap` | `double` | Yes | — | Space between two boxes. |
| `fills` | `List<double>` | Yes | — | One fraction per shape: 0, 0.5 or 1. |
| `selected` | `Color?` | Yes | — | Fill of the selected portion. |
| `unselected` | `Color?` | Yes | — | Fill of the unselected portion, or null when it is drawn as an outline with no fill at all. |
| `unselectedStroke` | `Color?` | Yes | — | Outline of the unselected portion. |
| `strokeWidth` | `double` | Yes | — | Outline width. Zero suppresses the outline entirely. |

#### State, callback, and accessibility fields

- `selected` (`Color?`): Fill of the selected portion.

### `FluentRatingShape`

Which silhouette is drawn. Figma's `Shape` axis.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

```dart
enum FluentRatingShape {
  star,
  circle,
  square,
}
```

### `FluentRatingSize`

Shape box edge length. Figma's `Size` axis.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

```dart
enum FluentRatingSize {
  small,
  medium,
  large,
  extraLarge,
}
```

### `FluentRatingState`

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

#### Constructor: `FluentRatingState`

```dart
const FluentRatingState({
    required super.value,
    required super.max,
    required super.compact,
    required super.enabled,
    required this.type,
    required this.size,
    required this.shape,
    required this.color,
    super.step,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `value` | `double` | Yes | — | The rating being shown, rounded to the nearest half. |
| `max` | `int` | Yes | — | How many shapes a full rating is worth. |
| `compact` | `bool` | Yes | — | Whether a single filled shape stands in for the whole row. |
| `enabled` | `bool` | Yes | — | Whether the rating responds to input. |
| `type` | `FluentRatingType` | Yes | — | Whether the rating takes input. |
| `size` | `FluentRatingSize` | Yes | — | Shape box edge length. |
| `shape` | `FluentRatingShape` | Yes | — | Which silhouette is drawn. |
| `color` | `FluentRatingColor` | Yes | — | Which colour family the shapes take. |
| `step` | `double` | No | `1` | The granularity the control commits at: 1, or 0.5 for half values. |

### `FluentRatingStyle`

Source: `packages/fluent_2/lib/src/inputs/rating_style.dart`

#### Constructor: `FluentRatingStyle`

```dart
const FluentRatingStyle({
    this.itemSize,
    this.shape,
    this.gap,
    this.selectedColor,
    this.unselectedColor,
    this.unselectedStrokeColor,
    this.strokeWidth,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `itemSize` | `WidgetStateProperty<double?>?` | No | `null` | Edge length of one shape's box. 12, 16, 20 or 28 per the size ramp. |
| `shape` | `WidgetStateProperty<FluentRatingShape?>?` | No | `null` | Which of the three silhouettes is drawn. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between two boxes. `FluentSpacing.xxs` by default, which is what Figma's shape row binds its item spacing to at every size. |
| `selectedColor` | `WidgetStateProperty<Color?>?` | No | `null` | Fill of the selected portion of a shape. |
| `unselectedColor` | `WidgetStateProperty<Color?>?` | No | `null` | Fill of the unselected portion. |
| `unselectedStrokeColor` | `WidgetStateProperty<Color?>?` | No | `null` | Outline of the unselected portion. |
| `strokeWidth` | `WidgetStateProperty<double?>?` | No | `null` | Outline width. Zero means no outline, which is not the same as a transparent one — a zero-width outline cannot become visible in high contrast. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

#### State, callback, and accessibility fields

- `selectedColor` (`WidgetStateProperty<Color?>?`): Fill of the selected portion of a shape.

### `FluentRatingTheme`

Overrides the rating style for a subtree.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

#### Constructor: `FluentRatingTheme`

```dart
const FluentRatingTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentRatingStyle` | Yes | — | The style layered over the type, size, shape and colour defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentRatingType`

Whether the rating takes input or only reports a value. Figma's `Type` axis.

Source: `packages/fluent_2/lib/src/inputs/rating.dart`

```dart
enum FluentRatingType {
  interactive,
  display,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentRatingState resolveFluentRatingState({
  required double value,
  int max = 5,
  double step = 1,
  bool compact = false,
  bool enabled = true,
  FluentRatingType type = FluentRatingType.interactive,
  FluentRatingSize size = FluentRatingSize.extraLarge,
  FluentRatingShape shape = FluentRatingShape.star,
  FluentRatingColor color = FluentRatingColor.neutral,
});

FluentRatingStyle resolveFluentRatingStyle(
  FluentRatingState state,
  FluentThemeData theme,
);

Widget buildFluentRating(
  FluentRatingBaseState state,
  FluentRatingStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`:

```dart
FluentRating(
                  value: context.knobs.slider(
                    label: 'Rating',
                    initial: 3,
                    min: 0,
                    max: 5,
                  ),
                  onChanged: (value) {},
                )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/inputs/rating.dart`
- Tests: `packages/fluent_2/test/goldens/rating_golden_test.dart`, `packages/fluent_2/test/inputs/rating_test.dart`
- Stories: `packages/fluent_2/example/lib/storybook/components/inputs_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/rating/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
