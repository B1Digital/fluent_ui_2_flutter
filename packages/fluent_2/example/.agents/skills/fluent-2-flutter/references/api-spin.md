# Spin: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/spin/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentSpinButton`, `FluentSpinButtonStepper`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentSpinButton`

A Fluent 2 spin button: a numeric field with an up/down stepper column.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButton`

```dart
const FluentSpinButton({
    super.key,
    required this.value,
    this.onChanged,
    this.min,
    this.max,
    this.step = 1,
    this.pageStep = 1,
    this.precision,
    this.displayValue,
    this.placeholder,
    this.appearance = FluentSpinButtonAppearance.outline,
    this.size = FluentSpinButtonSize.medium,
    this.readOnly = false,
    this.invalid = false,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
    this.keyboardType = const TextInputType.numberWithOptions(
      signed: true,
      decimal: true,
    ),
    this.textInputAction = TextInputAction.done,
    this.onSubmitted,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `double?` | Yes | — | The current value, or null for an empty field. |
| `onChanged` | `ValueChanged<double?>?` | No | `null` | Called with the value a step or a commit landed on, already clamped and rounded. Null disables the control. |
| `min` | `double?` | No | `null` | Lower bound, or null for unbounded. Home jumps here when it is set. |
| `max` | `double?` | No | `null` | Upper bound, or null for unbounded. End jumps here when it is set. |
| `step` | `double` | No | `1` | Travel per Up or Down press. Also decides [precision] when that is null. |
| `pageStep` | `double` | No | `1` | Travel per PageUp or PageDown press. |
| `precision` | `int?` | No | `null` | Decimal places the value is rounded and rendered to. |
| `displayValue` | `String?` | No | `null` | Text to show instead of the formatted [value]. |
| `placeholder` | `String?` | No | `null` | Text shown while the field is empty. |
| `appearance` | `FluentSpinButtonAppearance` | No | `FluentSpinButtonAppearance.outline` | Fill and outline treatment. |
| `size` | `FluentSpinButtonSize` | No | `FluentSpinButtonSize.medium` | Height and type ramp. |
| `readOnly` | `bool` | No | `false` | Whether the value can be read and focused but not changed. |
| `invalid` | `bool` | No | `false` | Whether the value fails validation. Paints the danger border. |
| `style` | `FluentSpinButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology alongside the value. |
| `keyboardType` | `TextInputType` | No | `const TextInputType.numberWithOptions(<br> signed: true,<br> decimal: true,<br> )` | Soft-keyboard type. Numeric with a sign and a decimal point by default. |
| `textInputAction` | `TextInputAction` | No | `TextInputAction.done` | Soft-keyboard action key. |
| `onSubmitted` | `ValueChanged<double?>?` | No | `null` | Called with the committed value when the action key is pressed. |

#### State, callback, and accessibility fields

- `value` (`double?`): The current value, or null for an empty field.
- `onChanged` (`ValueChanged<double?>?`): Called with the value a step or a commit landed on, already clamped and rounded. Null disables the control.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology alongside the value.
- `onSubmitted` (`ValueChanged<double?>?`): Called with the committed value when the action key is pressed.

### `FluentSpinButtonStepper`

One half of a spin button's stepper column.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButtonStepper`

```dart
const FluentSpinButtonStepper({
    super.key,
    required this.direction,
    required this.style,
    this.onPressed,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `direction` | `FluentSpinButtonStepperDirection` | Yes | — | Which half this is. |
| `style` | `FluentSpinButtonStyle` | Yes | — | The resolved spin button style. Only the `stepper*`, `glyphSize` and `borderRadius` properties are read. |
| `onPressed` | `VoidCallback?` | No | `null` | Takes one step. A mouse or pen takes the first on the press and repeats while the button is held, as upstream's `useSpinButton` does; a finger takes one on release. Null makes the half inert, which is what a disabled, read-only or at-bound spin button does: upstream renders those buttons `disabled`. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Takes one step. A mouse or pen takes the first on the press and repeats while the button is held, as upstream's `useSpinButton` does; a finger takes one on release. Null makes the half inert, which is what a disabled, read-only or at-bound spin button does: upstream renders those buttons `disabled`.

## Related public types

### `FluentSpinButtonAppearance`

How a spin button is filled and outlined. Figma's `Style` axis.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

```dart
enum FluentSpinButtonAppearance {
  outline,
  underline,
  filledDarker,
  filledLighter,
}
```

### `FluentSpinButtonBaseState`

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButtonBaseState`

```dart
const FluentSpinButtonBaseState({
    required this.enabled,
    required this.readOnly,
    required this.invalid,
    required this.focused,
    required this.field,
    this.onIncrease,
    this.onDecrease,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the control responds to input at all. |
| `readOnly` | `bool` | Yes | — | Whether the value can be read and focused but not changed. |
| `invalid` | `bool` | Yes | — | Whether the value fails validation. Figma's `State=Error`. |
| `focused` | `bool` | Yes | — | Whether the field currently holds focus. |
| `field` | `Widget` | Yes | — | The editable text, already wired to its controller and focus node. |
| `onIncrease` | `VoidCallback?` | No | `null` | Adds one step. Null makes the increase half inert. |
| `onDecrease` | `VoidCallback?` | No | `null` | Subtracts one step. Null makes the decrease half inert. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the control responds to input at all.
- `onIncrease` (`VoidCallback?`): Adds one step. Null makes the increase half inert.
- `onDecrease` (`VoidCallback?`): Subtracts one step. Null makes the decrease half inert.

### `FluentSpinButtonChevronPainter`

Paints one stepper's chevron: upstream's `ChevronUp16Regular` / `ChevronDown16Regular`, from the svg's own path.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButtonChevronPainter`

```dart
const FluentSpinButtonChevronPainter({
    required this.direction,
    required this.color,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `direction` | `FluentSpinButtonStepperDirection` | Yes | — | Which way the chevron points. |
| `color` | `Color` | Yes | — | Chevron tone. A `neutralForeground3` ramp stop, never derived from the surface behind it. |

### `FluentSpinButtonSize`

Control height and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

```dart
enum FluentSpinButtonSize {
  medium,
  small,
}
```

### `FluentSpinButtonState`

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButtonState`

```dart
const FluentSpinButtonState({
    required super.enabled,
    required super.readOnly,
    required super.invalid,
    required super.focused,
    required super.field,
    required this.appearance,
    required this.size,
    super.onIncrease,
    super.onDecrease,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the control responds to input at all. |
| `readOnly` | `bool` | Yes | — | Whether the value can be read and focused but not changed. |
| `invalid` | `bool` | Yes | — | Whether the value fails validation. Figma's `State=Error`. |
| `focused` | `bool` | Yes | — | Whether the field currently holds focus. |
| `field` | `Widget` | Yes | — | The editable text, already wired to its controller and focus node. |
| `appearance` | `FluentSpinButtonAppearance` | Yes | — | Fill and outline treatment. |
| `size` | `FluentSpinButtonSize` | Yes | — | Height and type ramp. |
| `onIncrease` | `VoidCallback?` | No | `null` | Adds one step. Null makes the increase half inert. |
| `onDecrease` | `VoidCallback?` | No | `null` | Subtracts one step. Null makes the decrease half inert. |

### `FluentSpinButtonStepperDirection`

Which way one stepper half moves the value.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

```dart
enum FluentSpinButtonStepperDirection {
  increase,
  decrease,
}
```

### `FluentSpinButtonStyle`

Source: `packages/fluent_2/lib/src/inputs/spin_button_style.dart`

#### Constructor: `FluentSpinButtonStyle`

```dart
const FluentSpinButtonStyle({
    this.backgroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.bottomRuleColor,
    this.bottomRuleWidth,
    this.focusUnderlineColor,
    this.focusUnderlineWidth,
    this.foregroundColor,
    this.placeholderColor,
    this.cursorColor,
    this.selectionColor,
    this.stepperForegroundColor,
    this.stepperBackgroundColor,
    this.textStyle,
    this.padding,
    this.contentPadding,
    this.stepperSize,
    this.stepperPadding,
    this.glyphSize,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill behind the text and the steppers. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the border's top, left and right sides — and of the bottom too when [bottomRuleColor] is null. Null is no side border at all, which is not the same as transparent: Fluent's `transparentStroke` family becomes opaque in high contrast, which is the only thing outlining a filled input there. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Width of the border's top, left and right sides. Zero means no side border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the fill and of the steppers' outer corners. The border and the focus bar take it too, unless there is no side border: a bottom-only border is square, as upstream's underline is. |
| `bottomRuleColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the border's bottom side, joined to [borderColor] on the corner diagonal the way a browser joins two border colours. |
| `bottomRuleWidth` | `WidgetStateProperty<double?>?` | No | `null` | Width of the border's bottom side. |
| `focusUnderlineColor` | `WidgetStateProperty<Color?>?` | No | `null` | The brand rule that grows from the centre when the field takes focus. |
| `focusUnderlineWidth` | `WidgetStateProperty<double?>?` | No | `null` | Thickness of the focus rule. `FluentStroke.thick` upstream and in Figma. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the value text. |
| `placeholderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the placeholder text. A different token from [foregroundColor] — never the same colour at a lower opacity. |
| `cursorColor` | `WidgetStateProperty<Color?>?` | No | `null` | Caret colour. |
| `selectionColor` | `WidgetStateProperty<Color?>?` | No | `null` | Selection highlight colour. |
| `stepperForegroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Chevron colour on the stepper column. Resolved against the *stepper's* own interaction states, not the field's. |
| `stepperBackgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Fill behind one stepper, resolved against that stepper's own states. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Text ramp of the value and the placeholder alike. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset from the control's edge to the content row. Start only in Fluent — the stepper column sits flush against the end edge. |
| `contentPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the editable text itself, inside [padding]. |
| `stepperSize` | `WidgetStateProperty<Size?>?` | No | `null` | Size of one stepper. Both halves are the same size, and the column is two of them stacked. |
| `stepperPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset from one stepper's box to the box its chevron is centred in, stated for the increase half. The decrease half mirrors it top to bottom, so the larger inset always faces away from the middle of the column. |
| `glyphSize` | `WidgetStateProperty<double?>?` | No | `null` | Size of the chevron icon. It may be taller than the box [stepperPadding] leaves, and overflows it evenly, as upstream's svg does. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum size of the whole control. Only the height is a Fluent number; the width is whatever the caller gives it. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor over the text column, the full height of the control. |

### `FluentSpinButtonTheme`

Overrides the spin button style for a subtree.

Source: `packages/fluent_2/lib/src/inputs/spin_button.dart`

#### Constructor: `FluentSpinButtonTheme`

```dart
const FluentSpinButtonTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentSpinButtonStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentSpinButtonState resolveFluentSpinButtonState({
  required Widget field,
  bool enabled = true,
  bool readOnly = false,
  bool invalid = false,
  bool focused = false,
  FluentSpinButtonAppearance appearance = FluentSpinButtonAppearance.outline,
  FluentSpinButtonSize size = FluentSpinButtonSize.medium,
  VoidCallback? onIncrease,
  VoidCallback? onDecrease,
});

FluentSpinButtonStyle resolveFluentSpinButtonStyle(
  FluentSpinButtonState state,
  FluentThemeData theme,
);

Widget buildFluentSpinButton(
  FluentSpinButtonBaseState state,
  FluentSpinButtonStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/pages/charts_verticalbarchart.dart`:

```dart
FluentSpinButton(
                value: _barWidth,
                min: 1,
                max: 300,
                semanticLabel: 'barWidth',
                onChanged: (double? value) =>
                    setState(() => _barWidth = value ?? _barWidth),
              )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/inputs/spin_button.dart`
- Tests: `packages/fluent_2/test/goldens/spin_button_golden_test.dart`, `packages/fluent_2/test/inputs/selection_dismissal_test.dart`, `packages/fluent_2/test/inputs/spin_button_test.dart`, `packages/fluent_2/test/inputs/time_picker_test.dart`, `packages/fluent_2/test/internal/text_context_menu_test.dart`
- Stories: `packages/fluent_2/example/lib/pages/charts_verticalbarchart.dart`, `packages/fluent_2/example/lib/pages/charts_verticalstackedbarchart.dart`, `packages/fluent_2/example/lib/pages/components_carousel_carousel.dart`, `packages/fluent_2/example/lib/pages/components_colorpicker.dart`, `packages/fluent_2/example/lib/pages/components_field.dart`, `packages/fluent_2/example/lib/pages/components_spinbutton.dart`, `packages/fluent_2/example/lib/pages/components_toast.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/spin/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
