# Image: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/image/usage/

Design decisions and best practices: [components-identity-content.md](./components-identity-content.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **compose**. There is no one-to-one Fluent widget. Use the Flutter primitive and Fluent tokens described in the design guidance; do not invent a class.

Mapped Flutter API: `Image`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `Image`

A widget that displays an image.

Source: `package:flutter/src/widgets/image.dart`

#### Constructor: `Image`

```dart
const Image({
    super.key,
    required this.image,
    this.frameBuilder,
    this.loadingBuilder,
    this.errorBuilder,
    this.semanticLabel,
    this.excludeFromSemantics = false,
    this.width,
    this.height,
    this.color,
    this.opacity,
    this.colorBlendMode,
    this.fit,
    this.alignment = Alignment.center,
    this.repeat = ImageRepeat.noRepeat,
    this.centerSlice,
    this.matchTextDirection = false,
    this.gaplessPlayback = false,
    this.isAntiAlias = false,
    this.filterQuality = FilterQuality.medium,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `image` | `ImageProvider` | Yes | — | The image to display. |
| `frameBuilder` | `ImageFrameBuilder?` | No | `null` | A builder function responsible for creating the widget that represents this image. |
| `loadingBuilder` | `ImageLoadingBuilder?` | No | `null` | A builder that specifies the widget to display to the user while an image is still loading. |
| `errorBuilder` | `ImageErrorWidgetBuilder?` | No | `null` | A builder function that is called if an error occurs during image loading. |
| `semanticLabel` | `String?` | No | `null` | A Semantic description of the image. |
| `excludeFromSemantics` | `bool` | No | `false` | Whether to exclude this image from semantics. |
| `width` | `double?` | No | `null` | If non-null, require the image to have this width (in logical pixels). |
| `height` | `double?` | No | `null` | If non-null, require the image to have this height (in logical pixels). |
| `color` | `Color?` | No | `null` | If non-null, this color is blended with each image pixel using [colorBlendMode]. |
| `opacity` | `Animation<double>?` | No | `null` | If non-null, the value from the [Animation] is multiplied with the opacity of each image pixel before painting onto the canvas. |
| `colorBlendMode` | `BlendMode?` | No | `null` | Used to combine [color] with this image. |
| `fit` | `BoxFit?` | No | `null` | How to inscribe the image into the space allocated during layout. |
| `alignment` | `AlignmentGeometry` | No | `Alignment.center` | How to align the image within its bounds. |
| `repeat` | `ImageRepeat` | No | `ImageRepeat.noRepeat` | How to paint any portions of the layout bounds not covered by the image. |
| `centerSlice` | `Rect?` | No | `null` | The center slice for a nine-patch image. |
| `matchTextDirection` | `bool` | No | `false` | Whether to paint the image in the direction of the [TextDirection]. |
| `gaplessPlayback` | `bool` | No | `false` | Whether to continue showing the old image (true), or briefly show nothing (false), when the image provider changes. The default value is false. |
| `isAntiAlias` | `bool` | No | `false` | Whether to paint the image with anti-aliasing. |
| `filterQuality` | `FilterQuality` | No | `FilterQuality.medium` | The rendering quality of the image. |

#### Constructor: `Image.network`

```dart
Image.network(
    String src, {
    super.key,
    double scale = 1.0,
    this.frameBuilder,
    this.loadingBuilder,
    this.errorBuilder,
    this.semanticLabel,
    this.excludeFromSemantics = false,
    this.width,
    this.height,
    this.color,
    this.opacity,
    this.colorBlendMode,
    this.fit,
    this.alignment = Alignment.center,
    this.repeat = ImageRepeat.noRepeat,
    this.centerSlice,
    this.matchTextDirection = false,
    this.gaplessPlayback = false,
    this.filterQuality = FilterQuality.medium,
    this.isAntiAlias = false,
    Map<String, String>? headers,
    int? cacheWidth,
    int? cacheHeight,
    WebHtmlElementStrategy webHtmlElementStrategy = WebHtmlElementStrategy.never,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `src` | `String` | Yes | — | Network URL used as the image source. |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `scale` | `double` | No | `1.0` | Scale factor used when decoding and laying out the image. |
| `frameBuilder` | `ImageFrameBuilder?` | No | `null` | A builder function responsible for creating the widget that represents this image. |
| `loadingBuilder` | `ImageLoadingBuilder?` | No | `null` | A builder that specifies the widget to display to the user while an image is still loading. |
| `errorBuilder` | `ImageErrorWidgetBuilder?` | No | `null` | A builder function that is called if an error occurs during image loading. |
| `semanticLabel` | `String?` | No | `null` | A Semantic description of the image. |
| `excludeFromSemantics` | `bool` | No | `false` | Whether to exclude this image from semantics. |
| `width` | `double?` | No | `null` | If non-null, require the image to have this width (in logical pixels). |
| `height` | `double?` | No | `null` | If non-null, require the image to have this height (in logical pixels). |
| `color` | `Color?` | No | `null` | If non-null, this color is blended with each image pixel using [colorBlendMode]. |
| `opacity` | `Animation<double>?` | No | `null` | If non-null, the value from the [Animation] is multiplied with the opacity of each image pixel before painting onto the canvas. |
| `colorBlendMode` | `BlendMode?` | No | `null` | Used to combine [color] with this image. |
| `fit` | `BoxFit?` | No | `null` | How to inscribe the image into the space allocated during layout. |
| `alignment` | `AlignmentGeometry` | No | `Alignment.center` | How to align the image within its bounds. |
| `repeat` | `ImageRepeat` | No | `ImageRepeat.noRepeat` | How to paint any portions of the layout bounds not covered by the image. |
| `centerSlice` | `Rect?` | No | `null` | The center slice for a nine-patch image. |
| `matchTextDirection` | `bool` | No | `false` | Whether to paint the image in the direction of the [TextDirection]. |
| `gaplessPlayback` | `bool` | No | `false` | Whether to continue showing the old image (true), or briefly show nothing (false), when the image provider changes. The default value is false. |
| `filterQuality` | `FilterQuality` | No | `FilterQuality.medium` | The rendering quality of the image. |
| `isAntiAlias` | `bool` | No | `false` | Whether to paint the image with anti-aliasing. |
| `headers` | `Map<String, String>?` | No | `null` | Optional HTTP headers sent while loading a network image. |
| `cacheWidth` | `int?` | No | `null` | Requested decoded-image cache width in physical pixels. |
| `cacheHeight` | `int?` | No | `null` | Requested decoded-image cache height in physical pixels. |
| `webHtmlElementStrategy` | `WebHtmlElementStrategy` | No | `WebHtmlElementStrategy.never` | Flutter Web strategy for rendering the network image with an HTML element. |

#### Constructor: `Image.file`

```dart
Image.file(
    File file, {
    super.key,
    double scale = 1.0,
    this.frameBuilder,
    this.errorBuilder,
    this.semanticLabel,
    this.excludeFromSemantics = false,
    this.width,
    this.height,
    this.color,
    this.opacity,
    this.colorBlendMode,
    this.fit,
    this.alignment = Alignment.center,
    this.repeat = ImageRepeat.noRepeat,
    this.centerSlice,
    this.matchTextDirection = false,
    this.gaplessPlayback = false,
    this.isAntiAlias = false,
    this.filterQuality = FilterQuality.medium,
    int? cacheWidth,
    int? cacheHeight,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `file` | `File` | Yes | — | Local file used as the image source. |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `scale` | `double` | No | `1.0` | Scale factor used when decoding and laying out the image. |
| `frameBuilder` | `ImageFrameBuilder?` | No | `null` | A builder function responsible for creating the widget that represents this image. |
| `errorBuilder` | `ImageErrorWidgetBuilder?` | No | `null` | A builder function that is called if an error occurs during image loading. |
| `semanticLabel` | `String?` | No | `null` | A Semantic description of the image. |
| `excludeFromSemantics` | `bool` | No | `false` | Whether to exclude this image from semantics. |
| `width` | `double?` | No | `null` | If non-null, require the image to have this width (in logical pixels). |
| `height` | `double?` | No | `null` | If non-null, require the image to have this height (in logical pixels). |
| `color` | `Color?` | No | `null` | If non-null, this color is blended with each image pixel using [colorBlendMode]. |
| `opacity` | `Animation<double>?` | No | `null` | If non-null, the value from the [Animation] is multiplied with the opacity of each image pixel before painting onto the canvas. |
| `colorBlendMode` | `BlendMode?` | No | `null` | Used to combine [color] with this image. |
| `fit` | `BoxFit?` | No | `null` | How to inscribe the image into the space allocated during layout. |
| `alignment` | `AlignmentGeometry` | No | `Alignment.center` | How to align the image within its bounds. |
| `repeat` | `ImageRepeat` | No | `ImageRepeat.noRepeat` | How to paint any portions of the layout bounds not covered by the image. |
| `centerSlice` | `Rect?` | No | `null` | The center slice for a nine-patch image. |
| `matchTextDirection` | `bool` | No | `false` | Whether to paint the image in the direction of the [TextDirection]. |
| `gaplessPlayback` | `bool` | No | `false` | Whether to continue showing the old image (true), or briefly show nothing (false), when the image provider changes. The default value is false. |
| `isAntiAlias` | `bool` | No | `false` | Whether to paint the image with anti-aliasing. |
| `filterQuality` | `FilterQuality` | No | `FilterQuality.medium` | The rendering quality of the image. |
| `cacheWidth` | `int?` | No | `null` | Requested decoded-image cache width in physical pixels. |
| `cacheHeight` | `int?` | No | `null` | Requested decoded-image cache height in physical pixels. |

#### Constructor: `Image.asset`

```dart
Image.asset(
    String name, {
    super.key,
    AssetBundle? bundle,
    this.frameBuilder,
    this.errorBuilder,
    this.semanticLabel,
    this.excludeFromSemantics = false,
    double? scale,
    this.width,
    this.height,
    this.color,
    this.opacity,
    this.colorBlendMode,
    this.fit,
    this.alignment = Alignment.center,
    this.repeat = ImageRepeat.noRepeat,
    this.centerSlice,
    this.matchTextDirection = false,
    this.gaplessPlayback = false,
    this.isAntiAlias = false,
    String? package,
    this.filterQuality = FilterQuality.medium,
    int? cacheWidth,
    int? cacheHeight,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `name` | `String` | Yes | — | Asset name used to locate the image resource. |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `bundle` | `AssetBundle?` | No | `null` | Asset bundle from which the image resource is loaded. |
| `frameBuilder` | `ImageFrameBuilder?` | No | `null` | A builder function responsible for creating the widget that represents this image. |
| `errorBuilder` | `ImageErrorWidgetBuilder?` | No | `null` | A builder function that is called if an error occurs during image loading. |
| `semanticLabel` | `String?` | No | `null` | A Semantic description of the image. |
| `excludeFromSemantics` | `bool` | No | `false` | Whether to exclude this image from semantics. |
| `scale` | `double?` | No | `null` | Scale factor used when decoding and laying out the image. |
| `width` | `double?` | No | `null` | If non-null, require the image to have this width (in logical pixels). |
| `height` | `double?` | No | `null` | If non-null, require the image to have this height (in logical pixels). |
| `color` | `Color?` | No | `null` | If non-null, this color is blended with each image pixel using [colorBlendMode]. |
| `opacity` | `Animation<double>?` | No | `null` | If non-null, the value from the [Animation] is multiplied with the opacity of each image pixel before painting onto the canvas. |
| `colorBlendMode` | `BlendMode?` | No | `null` | Used to combine [color] with this image. |
| `fit` | `BoxFit?` | No | `null` | How to inscribe the image into the space allocated during layout. |
| `alignment` | `AlignmentGeometry` | No | `Alignment.center` | How to align the image within its bounds. |
| `repeat` | `ImageRepeat` | No | `ImageRepeat.noRepeat` | How to paint any portions of the layout bounds not covered by the image. |
| `centerSlice` | `Rect?` | No | `null` | The center slice for a nine-patch image. |
| `matchTextDirection` | `bool` | No | `false` | Whether to paint the image in the direction of the [TextDirection]. |
| `gaplessPlayback` | `bool` | No | `false` | Whether to continue showing the old image (true), or briefly show nothing (false), when the image provider changes. The default value is false. |
| `isAntiAlias` | `bool` | No | `false` | Whether to paint the image with anti-aliasing. |
| `package` | `String?` | No | `null` | Package namespace containing the image asset. |
| `filterQuality` | `FilterQuality` | No | `FilterQuality.medium` | The rendering quality of the image. |
| `cacheWidth` | `int?` | No | `null` | Requested decoded-image cache width in physical pixels. |
| `cacheHeight` | `int?` | No | `null` | Requested decoded-image cache height in physical pixels. |

#### Constructor: `Image.memory`

```dart
Image.memory(
    Uint8List bytes, {
    super.key,
    double scale = 1.0,
    this.frameBuilder,
    this.errorBuilder,
    this.semanticLabel,
    this.excludeFromSemantics = false,
    this.width,
    this.height,
    this.color,
    this.opacity,
    this.colorBlendMode,
    this.fit,
    this.alignment = Alignment.center,
    this.repeat = ImageRepeat.noRepeat,
    this.centerSlice,
    this.matchTextDirection = false,
    this.gaplessPlayback = false,
    this.isAntiAlias = false,
    this.filterQuality = FilterQuality.medium,
    int? cacheWidth,
    int? cacheHeight,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `bytes` | `Uint8List` | Yes | — | Encoded image bytes used as the image source. |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `scale` | `double` | No | `1.0` | Scale factor used when decoding and laying out the image. |
| `frameBuilder` | `ImageFrameBuilder?` | No | `null` | A builder function responsible for creating the widget that represents this image. |
| `errorBuilder` | `ImageErrorWidgetBuilder?` | No | `null` | A builder function that is called if an error occurs during image loading. |
| `semanticLabel` | `String?` | No | `null` | A Semantic description of the image. |
| `excludeFromSemantics` | `bool` | No | `false` | Whether to exclude this image from semantics. |
| `width` | `double?` | No | `null` | If non-null, require the image to have this width (in logical pixels). |
| `height` | `double?` | No | `null` | If non-null, require the image to have this height (in logical pixels). |
| `color` | `Color?` | No | `null` | If non-null, this color is blended with each image pixel using [colorBlendMode]. |
| `opacity` | `Animation<double>?` | No | `null` | If non-null, the value from the [Animation] is multiplied with the opacity of each image pixel before painting onto the canvas. |
| `colorBlendMode` | `BlendMode?` | No | `null` | Used to combine [color] with this image. |
| `fit` | `BoxFit?` | No | `null` | How to inscribe the image into the space allocated during layout. |
| `alignment` | `AlignmentGeometry` | No | `Alignment.center` | How to align the image within its bounds. |
| `repeat` | `ImageRepeat` | No | `ImageRepeat.noRepeat` | How to paint any portions of the layout bounds not covered by the image. |
| `centerSlice` | `Rect?` | No | `null` | The center slice for a nine-patch image. |
| `matchTextDirection` | `bool` | No | `false` | Whether to paint the image in the direction of the [TextDirection]. |
| `gaplessPlayback` | `bool` | No | `false` | Whether to continue showing the old image (true), or briefly show nothing (false), when the image provider changes. The default value is false. |
| `isAntiAlias` | `bool` | No | `false` | Whether to paint the image with anti-aliasing. |
| `filterQuality` | `FilterQuality` | No | `FilterQuality.medium` | The rendering quality of the image. |
| `cacheWidth` | `int?` | No | `null` | Requested decoded-image cache width in physical pixels. |
| `cacheHeight` | `int?` | No | `null` | Requested decoded-image cache height in physical pixels. |

#### State, callback, and accessibility fields

- `semanticLabel` (`String?`): A Semantic description of the image.

## Related public types

No component-specific public Fluent types are referenced by these fields.

## Verified usage

No dedicated checked-in constructor excerpt is available. Use the exact
signatures above and verify any new example with Dart analysis and a widget test.

## Source and test evidence

- Implementation: `package:flutter/src/widgets/image.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/image/usage/
- Design decisions: `references/components-identity-content.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
