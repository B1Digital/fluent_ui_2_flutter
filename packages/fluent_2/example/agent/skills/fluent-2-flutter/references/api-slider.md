# Slider: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/slider/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentSlider`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentSlider`

A Fluent 2 slider.

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

#### Constructor: `FluentSlider`

```dart
const FluentSlider({
    super.key,
    required this.value,
    this.onChanged,
    this.min = 0,
    this.max = 100,
    this.step,
    this.size = FluentSliderSize.medium,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
    this.semanticFormatter,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `double` | Yes | — | The current value. Clamped into [min]…[max] for rendering. |
| `onChanged` | `ValueChanged<double>?` | No | `null` | Called with the value a drag or a key landed on. Null disables the slider. |
| `min` | `double` | No | `0` | The value at the start of the rail. |
| `max` | `double` | No | `100` | The value at the end of the rail. |
| `step` | `double?` | No | `null` | The interval between selectable values. Null leaves the rail continuous and unticked. |
| `size` | `FluentSliderSize` | No | `FluentSliderSize.medium` | Thumb diameter and rail thickness. |
| `style` | `FluentSliderStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology alongside the value. |
| `semanticFormatter` | `String Function(double value)?` | No | `null` | Formats a value for assistive technology. |

#### State, callback, and accessibility fields

- `value` (`double`): The current value. Clamped into [min]…[max] for rendering.
- `onChanged` (`ValueChanged<double>?`): Called with the value a drag or a key landed on. Null disables the slider.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology alongside the value.

## Related public types

### `FluentSliderBaseState`

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

#### Constructor: `FluentSliderBaseState`

```dart
const FluentSliderBaseState({
    required this.value,
    required this.min,
    required this.max,
    required this.enabled,
    this.step,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `value` | `double` | Yes | — | The current value, in [min]…[max]. Values outside are clamped rather than overflowing the rail. |
| `min` | `double` | Yes | — | The value at the start of the rail. |
| `max` | `double` | Yes | — | The value at the end of the rail. |
| `enabled` | `bool` | Yes | — | Whether the slider responds to input. |
| `step` | `double?` | No | `null` | The interval between selectable values, or null for a continuous slider. |

#### State, callback, and accessibility fields

- `value` (`double`): The current value, in [min]…[max]. Values outside are clamped rather than overflowing the rail.
- `enabled` (`bool`): Whether the slider responds to input.

### `FluentSliderPainter`

Paints a slider's rail, progress, ticks and thumb.

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

#### Constructor: `FluentSliderPainter`

```dart
const FluentSliderPainter({
    required this.fraction,
    required this.textDirection,
    required this.railThickness,
    required this.railInset,
    required this.railRadius,
    required this.thumbSize,
    required this.thumbInnerRadius,
    required this.thumbBorderWidth,
    this.intervals,
    this.railColor,
    this.progressColor,
    this.stepColor,
    this.thumbColor,
    this.thumbInnerColor,
    this.thumbBorderColor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `fraction` | `double` | Yes | — | Where the thumb sits along the rail, from 0 to 1. |
| `textDirection` | `TextDirection` | Yes | — | Which end of the rail the minimum is at. |
| `railThickness` | `double` | Yes | — | Rail height. |
| `railInset` | `double` | Yes | — | How far the rail stops short of each end of the control. The thumb's centre travels between the two insets, less one [thumbInnerRadius] at each end — upstream's `clamp(innerThumbRadius, progress, 100% - …)`. |
| `railRadius` | `BorderRadius` | Yes | — | Rail corner radius. |
| `thumbSize` | `double` | Yes | — | Thumb diameter, outline included. |
| `thumbInnerRadius` | `double` | Yes | — | Radius of the thumb's centre disc. |
| `thumbBorderWidth` | `double` | Yes | — | Thumb outline width. |
| `intervals` | `int?` | No | `null` | How many intervals to tick, or null for a continuous rail. |
| `railColor` | `Color?` | No | `null` | The unfilled part of the rail. |
| `progressColor` | `Color?` | No | `null` | The filled part of the rail. |
| `stepColor` | `Color?` | No | `null` | The tick marks. |
| `thumbColor` | `Color?` | No | `null` | The thumb's centre disc. |
| `thumbInnerColor` | `Color?` | No | `null` | The ring between [thumbColor] and [thumbBorderColor]. |
| `thumbBorderColor` | `Color?` | No | `null` | The thumb's outline. |

### `FluentSliderSize`

Thumb diameter and rail thickness. Figma's `Size` axis.

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

```dart
enum FluentSliderSize {
  medium,
  small,
}
```

### `FluentSliderState`

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

#### Constructor: `FluentSliderState`

```dart
const FluentSliderState({
    required super.value,
    required super.min,
    required super.max,
    required super.enabled,
    required this.size,
    super.step,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `value` | `double` | Yes | — | The current value, in [min]…[max]. Values outside are clamped rather than overflowing the rail. |
| `min` | `double` | Yes | — | The value at the start of the rail. |
| `max` | `double` | Yes | — | The value at the end of the rail. |
| `enabled` | `bool` | Yes | — | Whether the slider responds to input. |
| `size` | `FluentSliderSize` | Yes | — | Thumb diameter and rail thickness. |
| `step` | `double?` | No | `null` | The interval between selectable values, or null for a continuous slider. |

### `FluentSliderStyle`

Source: `packages/fluent_2/lib/src/inputs/slider_style.dart`

#### Constructor: `FluentSliderStyle`

```dart
const FluentSliderStyle({
    this.railColor,
    this.progressColor,
    this.stepColor,
    this.thumbColor,
    this.thumbInnerColor,
    this.thumbBorderColor,
    this.railThickness,
    this.railInset,
    this.railRadius,
    this.thumbSize,
    this.thumbInnerRadius,
    this.thumbBorderWidth,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `railColor` | `WidgetStateProperty<Color?>?` | No | `null` | The unfilled part of the rail. Upstream's `sliderRailColorVar`. |
| `progressColor` | `WidgetStateProperty<Color?>?` | No | `null` | The filled part of the rail, from the minimum up to the thumb. Upstream's `sliderProgressColorVar`. |
| `stepColor` | `WidgetStateProperty<Color?>?` | No | `null` | The tick marks a stepped slider draws across the rail. |
| `thumbColor` | `WidgetStateProperty<Color?>?` | No | `null` | The thumb's centre disc. Upstream's `sliderThumbColorVar`. |
| `thumbInnerColor` | `WidgetStateProperty<Color?>?` | No | `null` | The ring between [thumbColor] and [thumbBorderColor]. |
| `thumbBorderColor` | `WidgetStateProperty<Color?>?` | No | `null` | The thumb's outline. |
| `railThickness` | `WidgetStateProperty<double?>?` | No | `null` | Rail height. |
| `railInset` | `WidgetStateProperty<double?>?` | No | `null` | How far the rail stops short of each end of the control. |
| `railRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Rail corner radius. |
| `thumbSize` | `WidgetStateProperty<double?>?` | No | `null` | Thumb diameter, outline included. |
| `thumbInnerRadius` | `WidgetStateProperty<double?>?` | No | `null` | Radius of the thumb's centre disc. |
| `thumbBorderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Thumb outline width. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum size of the whole control. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentSliderTheme`

Overrides the slider style for a subtree.

Source: `packages/fluent_2/lib/src/inputs/slider.dart`

#### Constructor: `FluentSliderTheme`

```dart
const FluentSliderTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentSliderStyle` | Yes | — | The style layered over the size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentSliderState resolveFluentSliderState({
  required double value,
  double min = 0,
  double max = 100,
  double? step,
  bool enabled = true,
  FluentSliderSize size = FluentSliderSize.medium,
});

FluentSliderStyle resolveFluentSliderStyle(
  FluentSliderState state,
  FluentThemeData theme,
);

Widget buildFluentSlider(
  FluentSliderBaseState state,
  FluentSliderStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/gallery/gallery_app.dart`:

```dart
FluentSlider(
            value: (value as double?) ?? min,
            min: min,
            max: max,
            step: step,
            onChanged: (v) => onChanged(knob.id, v),
          )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/inputs/slider.dart`
- Tests: `packages/fluent_2/test/goldens/slider_golden_test.dart`, `packages/fluent_2/test/inputs/slider_test.dart`
- Stories: `packages/fluent_2/example/lib/gallery/gallery_app.dart`, `packages/fluent_2/example/lib/stories/field_stories.dart`, `packages/fluent_2/example/lib/stories/slider_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/slider/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
