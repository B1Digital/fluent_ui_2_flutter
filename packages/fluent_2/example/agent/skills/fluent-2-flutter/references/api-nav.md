# Nav: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/nav/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentNav`, `FluentNavAppItem`, `FluentNavCategory`, `FluentNavDivider`, `FluentNavItem`, `FluentNavSubItem`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentNav`

A Fluent 2 side navigation.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNav`

```dart
const FluentNav({
    super.key,
    required this.children,
    this.size = FluentNavSize.medium,
    this.selectedValue,
    this.defaultSelectedValue,
    this.onSelect,
    this.openCategories,
    this.defaultOpenCategories = const <Object>{},
    this.onOpenChange,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `children` | `List<Widget>` | Yes | — | The rows, in order. |
| `size` | `FluentNavSize` | No | `FluentNavSize.medium` | Row height and density. |
| `selectedValue` | `Object?` | No | `null` | The selected row's value, when the caller owns it. |
| `defaultSelectedValue` | `Object?` | No | `null` | The initially selected value while uncontrolled. |
| `onSelect` | `ValueChanged<Object>?` | No | `null` | Called with the value the nav is moving to, before it is applied. |
| `openCategories` | `Set<Object>?` | No | `null` | The open category set, when the caller owns it. |
| `defaultOpenCategories` | `Set<Object>` | No | `const <Object>{}` | The initially open categories while uncontrolled. |
| `onOpenChange` | `ValueChanged<Set<Object>>?` | No | `null` | Called with the open set the nav is moving to, before it is applied. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the name of this navigation region. |

#### State, callback, and accessibility fields

- `selectedValue` (`Object?`): The selected row's value, when the caller owns it.
- `onSelect` (`ValueChanged<Object>?`): Called with the value the nav is moving to, before it is applied.
- `openCategories` (`Set<Object>?`): The open category set, when the caller owns it.
- `onOpenChange` (`ValueChanged<Set<Object>>?`): Called with the open set the nav is moving to, before it is applied.
- `semanticLabel` (`String?`): Announced by assistive technology as the name of this navigation region.

### `FluentNavAppItem`

The product header at the top of a [FluentNav].

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavAppItem`

```dart
const FluentNavAppItem({
    super.key,
    required this.child,
    this.icon,
    this.onPressed,
    this.enabled = true,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The product name. |
| `icon` | `Widget?` | No | `null` | The product logo. 32 in medium density, 24 in small. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. |
| `style` | `FluentNavItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter.
- `enabled` (`bool`): Whether the row responds to input.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.

### `FluentNavCategory`

A collapsible group of [FluentNavSubItem]s.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavCategory`

```dart
const FluentNavCategory({
    super.key,
    required this.value,
    required this.child,
    required this.children,
    this.icon,
    this.enabled = true,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `Object` | Yes | — | Identifies this category in the nav's open set. |
| `child` | `Widget` | Yes | — | The header label. |
| `children` | `List<Widget>` | Yes | — | The sub-items, revealed while the category is open. |
| `icon` | `Widget?` | No | `null` | Leading icon. |
| `enabled` | `bool` | No | `true` | Whether the header responds to input. |
| `style` | `FluentNavItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `value` (`Object`): Identifies this category in the nav's open set.
- `enabled` (`bool`): Whether the header responds to input.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.

### `FluentNavDivider`

A rule between two groups of nav rows.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavDivider`

```dart
const FluentNavDivider({super.key, this.appearance});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `appearance` | `FluentDividerAppearance?` | No | `null` | How strongly the rule reads. Defaults to [FluentDivider]'s own default. |

### `FluentNavItem`

A leaf destination in a [FluentNav].

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavItem`

```dart
const FluentNavItem({
    super.key,
    required this.value,
    required this.child,
    this.icon,
    this.secondaryActions = const <Widget>[],
    this.enabled = true,
    this.onPressed,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `Object` | Yes | — | Identifies this row in the nav's selection. Anything with a stable `==`. |
| `child` | `Widget` | Yes | — | The label. |
| `icon` | `Widget?` | No | `null` | Leading icon. |
| `secondaryActions` | `List<Widget>` | No | `const <Widget>[]` | Trailing controls, each expected to be a 24-high icon button — compose `FluentButton.icon` with `FluentButtonSize.small` rather than rolling one. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. |
| `onPressed` | `VoidCallback?` | No | `null` | Called in addition to selecting the row. |
| `style` | `FluentNavItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `value` (`Object`): Identifies this row in the nav's selection. Anything with a stable `==`.
- `enabled` (`bool`): Whether the row responds to input.
- `onPressed` (`VoidCallback?`): Called in addition to selecting the row.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.

### `FluentNavSubItem`

A destination nested inside a [FluentNavCategory].

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavSubItem`

```dart
const FluentNavSubItem({
    super.key,
    required this.value,
    required this.child,
    this.secondaryActions = const <Widget>[],
    this.enabled = true,
    this.onPressed,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `Object` | Yes | — | Identifies this row in the nav's selection. |
| `child` | `Widget` | Yes | — | The label. |
| `secondaryActions` | `List<Widget>` | No | `const <Widget>[]` | Trailing controls, each expected to be a 24-high icon button. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. |
| `onPressed` | `VoidCallback?` | No | `null` | Called in addition to selecting the row. |
| `style` | `FluentNavItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `value` (`Object`): Identifies this row in the nav's selection.
- `enabled` (`bool`): Whether the row responds to input.
- `onPressed` (`VoidCallback?`): Called in addition to selecting the row.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.

## Related public types

### `FluentDividerAppearance`

How strongly a divider reads against its surface.

Source: `packages/fluent_2/lib/src/surfaces/divider.dart`

```dart
enum FluentDividerAppearance {
  standard,
  subtle,
  strong,
  brand,
}
```

### `FluentNavCollapseIntent`

Requests that the focused category close.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavCollapseIntent`

```dart
const FluentNavCollapseIntent();
```

### `FluentNavExpandIntent`

Requests that the focused category open.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavExpandIntent`

```dart
const FluentNavExpandIntent();
```

### `FluentNavItemBaseState`

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavItemBaseState`

```dart
const FluentNavItemBaseState({
    required this.enabled,
    required this.selected,
    required this.expanded,
    required this.expandable,
    required this.showIndicator,
    this.icon,
    this.label,
    this.secondaryActions = const <Widget>[],
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `selected` | `bool` | Yes | — | Whether this row is the current destination. Figma's `Selected` axis. |
| `expanded` | `bool` | Yes | — | Whether the category below this header is open. Figma's `Open` axis. Always false for a row that cannot expand. |
| `expandable` | `bool` | Yes | — | Whether this row carries a chevron and can be opened at all. |
| `showIndicator` | `bool` | Yes | — | Whether the row reserves the leading selection-indicator column. False only for [FluentNavItemKind.appItem], which has no indicator in Figma. |
| `icon` | `Widget?` | No | `null` | Leading icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. |
| `secondaryActions` | `List<Widget>` | No | `const <Widget>[]` | Trailing controls. Figma's `Secondary actions` axis; each is expected to be a 24-high icon button. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the row responds to input.
- `selected` (`bool`): Whether this row is the current destination. Figma's `Selected` axis.
- `expanded` (`bool`): Whether the category below this header is open. Figma's `Open` axis. Always false for a row that cannot expand.

### `FluentNavItemState`

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavItemState`

```dart
const FluentNavItemState({
    required super.enabled,
    required super.selected,
    required super.expanded,
    required super.expandable,
    required super.showIndicator,
    required this.kind,
    required this.size,
    super.icon,
    super.label,
    super.secondaryActions,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `selected` | `bool` | Yes | — | Whether this row is the current destination. Figma's `Selected` axis. |
| `expanded` | `bool` | Yes | — | Whether the category below this header is open. Figma's `Open` axis. Always false for a row that cannot expand. |
| `expandable` | `bool` | Yes | — | Whether this row carries a chevron and can be opened at all. |
| `showIndicator` | `bool` | Yes | — | Whether the row reserves the leading selection-indicator column. False only for [FluentNavItemKind.appItem], which has no indicator in Figma. |
| `kind` | `FluentNavItemKind` | Yes | — | Which component set this row is rendered from. |
| `size` | `FluentNavSize` | Yes | — | Row height and density. |
| `icon` | `Widget?` | No | `null` | Leading icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. |
| `secondaryActions` | `List<Widget>` | No | `const <Widget>[]` | Trailing controls. Figma's `Secondary actions` axis; each is expected to be a 24-high icon button. |

### `FluentNavItemStyle`

Source: `packages/fluent_2/lib/src/navigation/nav_style.dart`

#### Constructor: `FluentNavItemStyle`

```dart
const FluentNavItemStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.iconColor,
    this.indicatorColor,
    this.textStyle,
    this.borderRadius,
    this.indicatorRadius,
    this.padding,
    this.outerPadding,
    this.gap,
    this.indicatorGap,
    this.indicatorSize,
    this.iconSize,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Fill of the inner `Button` box. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label colour. |
| `iconColor` | `WidgetStateProperty<Color?>?` | No | `null` | Leading-icon colour. |
| `indicatorColor` | `WidgetStateProperty<Color?>?` | No | `null` | The selection indicator — Fluent's "french fry" — colour. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [foregroundColor]. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the inner `Button` box. |
| `indicatorRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the selection indicator. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the `Button` box. |
| `outerPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding of the outer frame, outside the indicator. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between icon, label and trailing slot. |
| `indicatorGap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the selection indicator and the `Button` box. |
| `indicatorSize` | `WidgetStateProperty<Size?>?` | No | `null` | Selection indicator size, width before height. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Icon edge length. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum size of the `Button` box. Only the height is load-bearing: a nav row fills the width it is given. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentNavItemTheme`

Overrides the nav row style for a subtree.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

#### Constructor: `FluentNavItemTheme`

```dart
const FluentNavItemTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentNavItemStyle` | Yes | — | The style layered over the kind and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentNavSize`

Row height and density. Figma ships this as two component sets rather than a variant axis — `NavNode - medium` and `NavNode - small`.

Source: `packages/fluent_2/lib/src/navigation/nav.dart`

```dart
enum FluentNavSize {
  medium,
  small,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentNavItemState resolveFluentNavItemState({
  required FluentNavItemKind kind,
  bool enabled = true,
  bool selected = false,
  bool expanded = false,
  bool expandable = false,
  FluentNavSize size = FluentNavSize.medium,
  Widget? icon,
  Widget? label,
  List<Widget> secondaryActions = const <Widget>[],
});

FluentNavItemStyle resolveFluentNavItemStyle(
  FluentNavItemState state,
  FluentThemeData theme,
);

Widget buildFluentNavItem(
  FluentNavItemBaseState state,
  FluentNavItemStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/stories/drawer_stories.dart`:

```dart
FluentNav(
            selectedValue: _selected,
            onSelect: (value) => setState(() => _selected = value),
            semanticLabel: 'Main navigation',
            children: <Widget>[
              for (final (value, icon, label) in _destinations)
                FluentNavItem(
                  value: value,
                  icon: Icon(icon),
                  child: Text(label),
                ),
            ],
          )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/navigation/nav.dart`
- Tests: `packages/fluent_2/test/goldens/nav_golden_test.dart`, `packages/fluent_2/test/navigation/nav_test.dart`
- Stories: `packages/fluent_2/example/lib/stories/drawer_stories.dart`, `packages/fluent_2/example/lib/stories/nav_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/nav/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
