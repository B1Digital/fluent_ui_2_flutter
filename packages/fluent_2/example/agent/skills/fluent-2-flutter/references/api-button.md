# Button: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/button/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentButton`, `FluentCompoundButton`, `FluentSplitButton`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentButton`

A Fluent 2 button.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

#### Constructor: `FluentButton`

```dart
const FluentButton({
    super.key,
    required this.child,
    this.onPressed,
    this.appearance = FluentButtonAppearance.secondary,
    this.size = FluentButtonSize.medium,
    this.shape = FluentButtonShape.rounded,
    this.iconPosition = FluentButtonIconPosition.before,
    this.icon,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget?` | Yes | — | The label. Null for an icon-only button. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. Null disables the button. |
| `appearance` | `FluentButtonAppearance` | No | `FluentButtonAppearance.secondary` | Fill and outline treatment. |
| `size` | `FluentButtonSize` | No | `FluentButtonSize.medium` | Height and type ramp. |
| `shape` | `FluentButtonShape` | No | `FluentButtonShape.rounded` | Corner treatment. |
| `iconPosition` | `FluentButtonIconPosition` | No | `FluentButtonIconPosition.before` | Which side of the label the icon sits on. |
| `icon` | `Widget?` | No | `null` | Optional leading or trailing icon. |
| `style` | `FluentButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology. |

#### Constructor: `FluentButton.icon`

```dart
const FluentButton.icon({
    super.key,
    required Widget this.icon,
    required String this.semanticLabel,
    this.onPressed,
    this.appearance = FluentButtonAppearance.secondary,
    this.size = FluentButtonSize.medium,
    this.shape = FluentButtonShape.rounded,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `icon` | `Widget` | Yes | — | Optional leading or trailing icon. |
| `semanticLabel` | `String` | Yes | — | Announced by assistive technology. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. Null disables the button. |
| `appearance` | `FluentButtonAppearance` | No | `FluentButtonAppearance.secondary` | Fill and outline treatment. |
| `size` | `FluentButtonSize` | No | `FluentButtonSize.medium` | Height and type ramp. |
| `shape` | `FluentButtonShape` | No | `FluentButtonShape.rounded` | Corner treatment. |
| `style` | `FluentButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter. Null disables the button.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology.

### `FluentCompoundButton`

A Fluent 2 compound button: a button with a second, explanatory line.

Source: `packages/fluent_2/lib/src/buttons/compound_button.dart`

#### Constructor: `FluentCompoundButton`

```dart
const FluentCompoundButton({
    super.key,
    required this.child,
    this.secondaryContent,
    this.onPressed,
    this.appearance = FluentButtonAppearance.secondary,
    this.size = FluentButtonSize.medium,
    this.shape = FluentButtonShape.rounded,
    this.iconPosition = FluentButtonIconPosition.before,
    this.icon,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The first, louder line. |
| `secondaryContent` | `Widget?` | No | `null` | The second, quieter line. Null renders a plain button with compound geometry. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter. Null disables the button. |
| `appearance` | `FluentButtonAppearance` | No | `FluentButtonAppearance.secondary` | Fill and outline treatment. |
| `size` | `FluentButtonSize` | No | `FluentButtonSize.medium` | Height and type ramp. |
| `shape` | `FluentButtonShape` | No | `FluentButtonShape.rounded` | Corner treatment. |
| `iconPosition` | `FluentButtonIconPosition` | No | `FluentButtonIconPosition.before` | Which side of the label the icon sits on. |
| `icon` | `Widget?` | No | `null` | Optional leading or trailing icon, rendered at 40 logical pixels. |
| `style` | `FluentCompoundButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter. Null disables the button.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `semanticLabel` (`String?`): Announced by assistive technology.

### `FluentSplitButton`

A Fluent 2 split button: a default action and a menu, in one container.

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButton`

```dart
const FluentSplitButton({
    super.key,
    required this.child,
    required this.menuSemanticLabel,
    this.onPressed,
    this.onMenuPressed,
    this.appearance = FluentButtonAppearance.secondary,
    this.size = FluentButtonSize.medium,
    this.shape = FluentButtonShape.rounded,
    this.iconPosition = FluentButtonIconPosition.before,
    this.icon,
    this.menuIcon,
    this.style,
    this.focusNode,
    this.menuFocusNode,
    this.autofocus = false,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The primary action's label. |
| `menuSemanticLabel` | `String` | Yes | — | Announced for the chevron half, which has no text of its own. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter on the primary half. Null disables that half. |
| `onMenuPressed` | `VoidCallback?` | No | `null` | Invoked on tap and on Space or Enter on the chevron half. Null disables that half. |
| `appearance` | `FluentButtonAppearance` | No | `FluentButtonAppearance.secondary` | Fill and outline treatment. |
| `size` | `FluentButtonSize` | No | `FluentButtonSize.medium` | Height and type ramp. |
| `shape` | `FluentButtonShape` | No | `FluentButtonShape.rounded` | Corner treatment. Applies to the pair's outer corners only. |
| `iconPosition` | `FluentButtonIconPosition` | No | `FluentButtonIconPosition.before` | Which side of the label the primary half's icon sits on. |
| `icon` | `Widget?` | No | `null` | Optional icon on the primary half. |
| `menuIcon` | `Widget?` | No | `null` | The chevron. Defaults to [fluentMenuChevron]. |
| `style` | `FluentSplitButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node for the primary half. One is created internally when omitted. |
| `menuFocusNode` | `FocusNode?` | No | `null` | Focus node for the chevron half. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether the primary half takes focus on mount. |
| `semanticLabel` | `String?` | No | `null` | Announced for the primary half. Optional; the label already carries the meaning. |

#### State, callback, and accessibility fields

- `onPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter on the primary half. Null disables that half.
- `onMenuPressed` (`VoidCallback?`): Invoked on tap and on Space or Enter on the chevron half. Null disables that half.
- `focusNode` (`FocusNode?`): Focus node for the primary half. One is created internally when omitted.
- `autofocus` (`bool`): Whether the primary half takes focus on mount.
- `semanticLabel` (`String?`): Announced for the primary half. Optional; the label already carries the meaning.

## Related public types

### `FluentButtonAppearance`

How a button is filled and outlined.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

```dart
enum FluentButtonAppearance {
  secondary,
  primary,
  outline,
  subtle,
  transparent,
}
```

### `FluentButtonBaseState`

Source: `packages/fluent_2/lib/src/buttons/button.dart`

#### Constructor: `FluentButtonBaseState`

```dart
const FluentButtonBaseState({
    required this.enabled,
    required this.iconPosition,
    this.icon,
    this.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the button responds to input.

### `FluentButtonIconPosition`

Which side of the label the icon sits on.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

```dart
enum FluentButtonIconPosition {
  before,
  after,
}
```

### `FluentButtonShape`

Corner treatment. Figma's `Shape` variable collection.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

```dart
enum FluentButtonShape {
  rounded,
  circular,
  square,
}
```

### `FluentButtonSize`

Button height and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

```dart
enum FluentButtonSize {
  small,
  medium,
  large,
}
```

### `FluentButtonState`

Source: `packages/fluent_2/lib/src/buttons/button.dart`

#### Constructor: `FluentButtonState`

```dart
const FluentButtonState({
    required super.enabled,
    required super.iconPosition,
    required this.appearance,
    required this.size,
    required this.shape,
    super.icon,
    super.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `appearance` | `FluentButtonAppearance` | Yes | — | Fill and outline treatment. |
| `size` | `FluentButtonSize` | Yes | — | Height and type ramp. |
| `shape` | `FluentButtonShape` | Yes | — | Corner treatment. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |

### `FluentButtonStyle`

Source: `packages/fluent_2/lib/src/buttons/button_style.dart`

#### Constructor: `FluentButtonStyle`

```dart
const FluentButtonStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.textStyle,
    this.padding,
    this.gap,
    this.iconSize,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label and icon colour. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [foregroundColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the border. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between icon and label. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Icon edge length. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum tap target. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentButtonTheme`

Overrides the button style for a subtree.

Source: `packages/fluent_2/lib/src/buttons/button.dart`

#### Constructor: `FluentButtonTheme`

```dart
const FluentButtonTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentButtonStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentCompoundButtonBaseState`

Source: `packages/fluent_2/lib/src/buttons/compound_button.dart`

#### Constructor: `FluentCompoundButtonBaseState`

```dart
const FluentCompoundButtonBaseState({
    required super.enabled,
    required super.iconPosition,
    super.icon,
    super.label,
    this.secondaryLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |
| `secondaryLabel` | `Widget?` | No | `null` | The second, quieter line under [FluentButtonBaseState.label]. |

### `FluentCompoundButtonState`

Source: `packages/fluent_2/lib/src/buttons/compound_button.dart`

#### Constructor: `FluentCompoundButtonState`

```dart
const FluentCompoundButtonState({
    required super.enabled,
    required super.iconPosition,
    required this.appearance,
    required this.size,
    required this.shape,
    super.icon,
    super.label,
    super.secondaryLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `appearance` | `FluentButtonAppearance` | Yes | — | Fill and outline treatment. Shared verbatim with `FluentButton`. |
| `size` | `FluentButtonSize` | Yes | — | Height and type ramp. Shared verbatim with `FluentButton`. |
| `shape` | `FluentButtonShape` | Yes | — | Corner treatment. Shared verbatim with `FluentButton`. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |
| `secondaryLabel` | `Widget?` | No | `null` | The second, quieter line under [FluentButtonBaseState.label]. |

### `FluentCompoundButtonStyle`

Source: `packages/fluent_2/lib/src/buttons/compound_button.dart`

#### Constructor: `FluentCompoundButtonStyle`

```dart
const FluentCompoundButtonStyle({
    this.button,
    this.secondaryColor,
    this.secondaryTextStyle,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `button` | `FluentButtonStyle?` | No | `null` | Everything the surface and the primary line share with `FluentButton`. |
| `secondaryColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the second line. |
| `secondaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Type ramp of the second line. Its colour is overridden by [secondaryColor]. |

### `FluentCompoundButtonTheme`

Overrides the compound button style for a subtree.

Source: `packages/fluent_2/lib/src/buttons/compound_button.dart`

#### Constructor: `FluentCompoundButtonTheme`

```dart
const FluentCompoundButtonTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentCompoundButtonStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentSplitButtonBaseState`

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonBaseState`

```dart
const FluentSplitButtonBaseState({
    required super.enabled,
    required super.iconPosition,
    required this.menuEnabled,
    super.icon,
    super.label,
    this.menuIcon,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `menuEnabled` | `bool` | Yes | — | Whether the chevron half responds to input. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |
| `menuIcon` | `Widget?` | No | `null` | The chevron. Defaults to [fluentMenuChevron] when null. |

### `FluentSplitButtonEdgeColors`

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonEdgeColors`

```dart
const FluentSplitButtonEdgeColors({
    required this.border,
    required this.divider,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `border` | `Color` | Yes | — | The outer border of this half. |
| `divider` | `Color` | Yes | — | The rule between the two halves. |

### `FluentSplitButtonEdgePainter`

Paints one half's border and, on the primary action half, the divider.

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonEdgePainter`

```dart
const FluentSplitButtonEdgePainter({
    required this.side,
    required this.borderColor,
    required this.borderWidth,
    required this.dividerColor,
    required this.radius,
    this.dividerWidth = FluentStroke.thin,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `side` | `FluentSplitButtonSide` | Yes | — | Which half is being painted. |
| `borderColor` | `Color` | Yes | — | Colour of the three outer sides. |
| `borderWidth` | `double` | Yes | — | Width of the three outer sides. Zero on the unbordered appearances, where nothing outside the divider is drawn. |
| `dividerColor` | `Color` | Yes | — | Colour of the rule between the halves. Painted only on [FluentSplitButtonSide.primaryAction] — upstream gives the chevron half `borderLeftWidth: 0` so the rule is drawn exactly once. |
| `radius` | `BorderRadius` | Yes | — | This half's corner radius, already squared off on the inner edge. |
| `dividerWidth` | `double` | No | `FluentStroke.thin` | Width of the rule. [FluentStroke.thin], matching upstream's `strokeWidthThin` border. |

### `FluentSplitButtonState`

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonState`

```dart
const FluentSplitButtonState({
    required super.enabled,
    required super.iconPosition,
    required super.menuEnabled,
    required this.appearance,
    required this.size,
    required this.shape,
    super.icon,
    super.label,
    super.menuIcon,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the button responds to input. |
| `iconPosition` | `FluentButtonIconPosition` | Yes | — | Which side the icon sits on. |
| `menuEnabled` | `bool` | Yes | — | Whether the chevron half responds to input. |
| `appearance` | `FluentButtonAppearance` | Yes | — | Fill and outline treatment. Shared verbatim with `FluentButton`. |
| `size` | `FluentButtonSize` | Yes | — | Height and type ramp. Shared verbatim with `FluentButton`. |
| `shape` | `FluentButtonShape` | Yes | — | Corner treatment. Shared verbatim with `FluentButton`. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A button with no label is an icon-only button. |
| `menuIcon` | `Widget?` | No | `null` | The chevron. Defaults to [fluentMenuChevron] when null. |

### `FluentSplitButtonStyle`

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonStyle`

```dart
const FluentSplitButtonStyle({this.button, this.dividerColor});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `button` | `FluentButtonStyle?` | No | `null` | Everything both halves share with `FluentButton`. |
| `dividerColor` | `WidgetStateProperty<Color?>?` | No | `null` | Colour of the 1px rule between the two halves, or null where Figma paints no rule at all — the subtle and transparent appearances, and primary while disabled. |

### `FluentSplitButtonTheme`

Overrides the split button style for a subtree.

Source: `packages/fluent_2/lib/src/buttons/split_button.dart`

#### Constructor: `FluentSplitButtonTheme`

```dart
const FluentSplitButtonTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentSplitButtonStyle` | Yes | — | The style layered over the appearance and size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentButtonState resolveFluentButtonState({
  bool enabled = true,
  FluentButtonAppearance appearance = FluentButtonAppearance.secondary,
  FluentButtonSize size = FluentButtonSize.medium,
  FluentButtonShape shape = FluentButtonShape.rounded,
  FluentButtonIconPosition iconPosition = FluentButtonIconPosition.before,
  Widget? icon,
  Widget? label,
});

FluentButtonStyle resolveFluentButtonStyle(
  FluentButtonState state,
  FluentThemeData theme,
);

FluentCompoundButtonState resolveFluentCompoundButtonState({
  bool enabled = true,
  FluentButtonAppearance appearance = FluentButtonAppearance.secondary,
  FluentButtonSize size = FluentButtonSize.medium,
  FluentButtonShape shape = FluentButtonShape.rounded,
  FluentButtonIconPosition iconPosition = FluentButtonIconPosition.before,
  Widget? icon,
  Widget? label,
  Widget? secondaryLabel,
});

FluentCompoundButtonStyle resolveFluentCompoundButtonStyle(
  FluentCompoundButtonState state,
  FluentThemeData theme,
);

FluentSplitButtonState resolveFluentSplitButtonState({
  bool enabled = true,
  bool menuEnabled = true,
  FluentButtonAppearance appearance = FluentButtonAppearance.secondary,
  FluentButtonSize size = FluentButtonSize.medium,
  FluentButtonShape shape = FluentButtonShape.rounded,
  FluentButtonIconPosition iconPosition = FluentButtonIconPosition.before,
  Widget? icon,
  Widget? label,
  Widget? menuIcon,
});

FluentSplitButtonStyle resolveFluentSplitButtonStyle(
  FluentSplitButtonState state,
  FluentThemeData theme, {
  required FluentSplitButtonSide side,
});

Widget buildFluentButton(
  FluentButtonBaseState state,
  FluentButtonStyle style,
  Set<WidgetState> states,
);

Widget buildFluentCompoundButton(
  FluentCompoundButtonBaseState state,
  FluentCompoundButtonStyle style,
  Set<WidgetState> states,
);

Widget buildFluentSplitButton(
  FluentSplitButtonBaseState state,
  FluentSplitButtonStyle style,
  Set<WidgetState> states, {
  required FluentSplitButtonSide side,
});
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2/example/lib/gallery/gallery_app.dart`:

```dart
FluentButton(
          appearance: FluentButtonAppearance.subtle,
          size: FluentButtonSize.small,
          onPressed: () => setState(() => _open = !open),
          child: Text(widget.section.component),
        )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/buttons/button.dart`, `packages/fluent_2/lib/src/buttons/compound_button.dart`, `packages/fluent_2/lib/src/buttons/split_button.dart`
- Tests: `packages/fluent_2/test/buttons/button_test.dart`, `packages/fluent_2/test/buttons/split_compound_button_test.dart`, `packages/fluent_2/test/goldens/button_golden_test.dart`, `packages/fluent_2/test/goldens/dialog_golden_test.dart`, `packages/fluent_2/test/goldens/field_golden_test.dart`, `packages/fluent_2/test/goldens/message_bar_golden_test.dart`, `packages/fluent_2/test/goldens/nav_golden_test.dart`, `packages/fluent_2/test/goldens/split_compound_button_golden_test.dart`, `packages/fluent_2/test/goldens/teaching_popover_golden_test.dart`, `packages/fluent_2/test/goldens/toolbar_golden_test.dart`, `packages/fluent_2/test/goldens/tree_golden_test.dart`, `packages/fluent_2/test/inputs/field_test.dart`
- Stories: `packages/fluent_2/example/lib/gallery/gallery_app.dart`, `packages/fluent_2/example/lib/stories/accordion_stories.dart`, `packages/fluent_2/example/lib/stories/acrylic_surface_stories.dart`, `packages/fluent_2/example/lib/stories/avatar_group_stories.dart`, `packages/fluent_2/example/lib/stories/avatar_stories.dart`, `packages/fluent_2/example/lib/stories/breadcrumb_stories.dart`, `packages/fluent_2/example/lib/stories/button_stories.dart`, `packages/fluent_2/example/lib/stories/card_stories.dart`, `packages/fluent_2/example/lib/stories/carousel_stories.dart`, `packages/fluent_2/example/lib/stories/compound_button_stories.dart`, `packages/fluent_2/example/lib/stories/data_grid_stories.dart`, `packages/fluent_2/example/lib/stories/dialog_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/button/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
