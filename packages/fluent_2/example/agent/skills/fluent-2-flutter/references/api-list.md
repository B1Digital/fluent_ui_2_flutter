# List: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/list/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentList`, `FluentListItem`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentList<T extends Object>`

A Fluent 2 list: the rows, and the selection they share.

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentList`

```dart
const FluentList({
    super.key,
    required this.items,
    this.selectedValues = const <Never>{},
    this.onSelectionChange,
    this.selection = FluentListSelection.none,
    this.size = FluentListItemSize.medium,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `items` | `List<FluentListItem<T>>` | Yes | — | The rows, in reading order. |
| `selectedValues` | `Set<T>` | No | `const <Never>{}` | The values currently selected. |
| `onSelectionChange` | `ValueChanged<Set<T>>?` | No | `null` | Invoked with the whole new selection. Null makes every row inert. |
| `selection` | `FluentListSelection` | No | `FluentListSelection.none` | Which affordance each row draws, and therefore whether selecting is additive. |
| `size` | `FluentListItemSize` | No | `FluentListItemSize.medium` | Row height and title type ramp for every row. |
| `style` | `FluentListItemStyle?` | No | `null` | Overrides layered over the theme defaults for every row. A row's own `style` is merged after this one. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the name of the list. |

#### State, callback, and accessibility fields

- `selectedValues` (`Set<T>`): The values currently selected.
- `onSelectionChange` (`ValueChanged<Set<T>>?`): Invoked with the whole new selection. Null makes every row inert.
- `semanticLabel` (`String?`): Announced by assistive technology as the name of the list.

### `FluentListItem<T extends Object>`

One row of a [FluentList].

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListItem`

```dart
const FluentListItem({
    super.key,
    required this.value,
    this.child,
    this.secondary,
    this.tertiary,
    this.media,
    this.trailing,
    this.enabled = true,
    this.style,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `T` | Yes | — | Identifies this row within its list. Compared with `==`. |
| `child` | `Widget?` | No | `null` | The title. |
| `secondary` | `Widget?` | No | `null` | The second line. Its presence makes this a two-line row. |
| `tertiary` | `Widget?` | No | `null` | Trailing metadata on the title's line — a timestamp, a count. |
| `media` | `Widget?` | No | `null` | The leading media slot, after the selection affordance. |
| `trailing` | `Widget?` | No | `null` | Trailing controls at the end of the row. |
| `enabled` | `bool` | No | `true` | Whether the row can be selected. |
| `style` | `FluentListItemStyle?` | No | `null` | Overrides layered over the list's style. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology in place of the row's own text. |

#### State, callback, and accessibility fields

- `value` (`T`): Identifies this row within its list. Compared with `==`.
- `enabled` (`bool`): Whether the row can be selected.
- `semanticLabel` (`String?`): Announced by assistive technology in place of the row's own text.

## Related public types

### `FluentListEdgeIntent`

Moves keyboard focus to the first or last enabled row.

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListEdgeIntent`

```dart
const FluentListEdgeIntent({required this.last});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `last` | `bool` | Yes | — | Whether to jump to the last row rather than the first. |

### `FluentListItemBaseState`

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListItemBaseState`

```dart
const FluentListItemBaseState({
    required this.enabled,
    required this.selected,
    this.indicator,
    this.media,
    this.primary,
    this.secondary,
    this.tertiary,
    this.trailing,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `selected` | `bool` | Yes | — | Whether the row is selected. Figma's `Active` axis, which moves both the fill and the title's weight. |
| `indicator` | `Widget?` | No | `null` | The selection affordance, already built and made inert. Null when the list is [FluentListSelection.none]. |
| `media` | `Widget?` | No | `null` | The leading media slot — an avatar, an icon, a thumbnail. |
| `primary` | `Widget?` | No | `null` | The title. |
| `secondary` | `Widget?` | No | `null` | The second line. Its presence is what makes this a two-line row, which is Figma's `ListItem type` axis. |
| `tertiary` | `Widget?` | No | `null` | Trailing metadata on the title's line — a timestamp, a count. |
| `trailing` | `Widget?` | No | `null` | Trailing controls, at the end of the row. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the row responds to input.
- `selected` (`bool`): Whether the row is selected. Figma's `Active` axis, which moves both the fill and the title's weight.

### `FluentListItemSize`

Row height and title type ramp. Figma's `Size` axis.

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

```dart
enum FluentListItemSize {
  small,
  medium,
}
```

### `FluentListItemState`

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListItemState`

```dart
const FluentListItemState({
    required super.enabled,
    required super.selected,
    required this.size,
    super.indicator,
    super.media,
    super.primary,
    super.secondary,
    super.tertiary,
    super.trailing,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `selected` | `bool` | Yes | — | Whether the row is selected. Figma's `Active` axis, which moves both the fill and the title's weight. |
| `size` | `FluentListItemSize` | Yes | — | Row height and title type ramp. |
| `indicator` | `Widget?` | No | `null` | The selection affordance, already built and made inert. Null when the list is [FluentListSelection.none]. |
| `media` | `Widget?` | No | `null` | The leading media slot — an avatar, an icon, a thumbnail. |
| `primary` | `Widget?` | No | `null` | The title. |
| `secondary` | `Widget?` | No | `null` | The second line. Its presence is what makes this a two-line row, which is Figma's `ListItem type` axis. |
| `tertiary` | `Widget?` | No | `null` | Trailing metadata on the title's line — a timestamp, a count. |
| `trailing` | `Widget?` | No | `null` | Trailing controls, at the end of the row. |

### `FluentListItemStyle`

Source: `packages/fluent_2/lib/src/navigation/list_item_style.dart`

#### Constructor: `FluentListItemStyle`

```dart
const FluentListItemStyle({
    this.backgroundColor,
    this.primaryTextColor,
    this.secondaryTextColor,
    this.tertiaryTextColor,
    this.primaryTextStyle,
    this.secondaryTextStyle,
    this.tertiaryTextStyle,
    this.borderRadius,
    this.padding,
    this.contentPadding,
    this.mediaPadding,
    this.tertiaryPadding,
    this.selectionPadding,
    this.selectionSize,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Row fill. `Neutral/Background/Subtle/*`, or `Neutral/Background/Disabled/Rest` while disabled. |
| `primaryTextColor` | `WidgetStateProperty<Color?>?` | No | `null` | Title colour. |
| `secondaryTextColor` | `WidgetStateProperty<Color?>?` | No | `null` | Second-line colour. |
| `tertiaryTextColor` | `WidgetStateProperty<Color?>?` | No | `null` | Trailing-metadata colour. |
| `primaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Title type ramp. Its colour is overridden by [primaryTextColor]. |
| `secondaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Second-line type ramp. Its colour is overridden by [secondaryTextColor]. |
| `tertiaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Trailing-metadata type ramp. Its colour is overridden by [tertiaryTextColor]. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius of the row. `Corner-radius/List/Default`. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset between the row's edge and its content. |
| `contentPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the text column. Its `left` is also the gap between the media slot and the title. |
| `mediaPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the leading media slot. |
| `tertiaryPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the trailing metadata. Its `left` is also the gap between the title and the metadata. |
| `selectionPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset around the selection affordance, which is what centres it on the row. |
| `selectionSize` | `WidgetStateProperty<double?>?` | No | `null` | Edge length of the selection affordance's box. Figma's `.Selection` frame is 24 at every list-item size. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum row size. Its height is the Figma variant's frame height. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering an enabled row. |

### `FluentListItemTheme`

Overrides the list item style for a subtree.

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListItemTheme`

```dart
const FluentListItemTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentListItemStyle` | Yes | — | The style layered over the size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentListMoveIntent`

Moves keyboard focus by [delta] rows, skipping disabled ones.

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

#### Constructor: `FluentListMoveIntent`

```dart
const FluentListMoveIntent(this.delta);
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `delta` | `int` | Yes | — | How many rows to move by. Negative moves towards the first row. |

### `FluentListSelection`

Which affordance a row draws for its selection, and therefore how the list behaves. Figma's `.Selection` `Type` axis, plus [none].

Source: `packages/fluent_2/lib/src/navigation/list_item.dart`

```dart
enum FluentListSelection {
  none,
  checkbox,
  radio,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentListItemState resolveFluentListItemState({
  bool enabled = true,
  bool selected = false,
  FluentListItemSize size = FluentListItemSize.medium,
  Widget? indicator,
  Widget? media,
  Widget? primary,
  Widget? secondary,
  Widget? tertiary,
  Widget? trailing,
});

FluentListItemStyle resolveFluentListItemStyle(
  FluentListItemState state,
  FluentThemeData theme,
);

Widget buildFluentListItem(
  FluentListItemBaseState state,
  FluentListItemStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

No dedicated checked-in constructor excerpt is available. Use the exact
signatures above and verify any new example with Dart analysis and a widget test.

## Source and test evidence

- Implementation: `packages/fluent_2/lib/src/navigation/list_item.dart`
- Tests: `packages/fluent_2/test/goldens/list_item_golden_test.dart`, `packages/fluent_2/test/navigation/list_item_test.dart`
- Stories: `packages/fluent_2/example/lib/stories/list_item_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/list/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
