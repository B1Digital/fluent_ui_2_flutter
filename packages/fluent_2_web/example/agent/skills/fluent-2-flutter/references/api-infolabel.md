# Info label: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/infolabel/usage/

Design decisions and best practices: [components-actions-inputs.md](./components-actions-inputs.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentInfoLabel`, `FluentInfoButton`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentInfoLabel`

A Fluent 2 info label — a [FluentLabel] with a [FluentInfoButton] after it.

Source: `packages/fluent_2_web/lib/src/inputs/info_label.dart`

#### Constructor: `FluentInfoLabel`

```dart
const FluentInfoLabel({
    super.key,
    required this.child,
    this.info,
    this.infoSemanticLabel,
    this.size = FluentLabelSize.medium,
    this.weight = FluentLabelWeight.regular,
    this.required = false,
    this.disabled = false,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.onOpenChanged,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The label text. |
| `info` | `Widget?` | No | `null` | The tip body. Null renders no trigger at all. |
| `infoSemanticLabel` | `String?` | No | `null` | Announced by assistive technology for the trigger. Required whenever [info] is set. |
| `size` | `FluentLabelSize` | No | `FluentLabelSize.medium` | Type ramp step, and the trigger's box with it. |
| `weight` | `FluentLabelWeight` | No | `FluentLabelWeight.regular` | Label font weight. |
| `required` | `bool` | No | `false` | Whether to render the required-field asterisk after [child]. |
| `disabled` | `bool` | No | `false` | Whether the label renders in its disabled colour and the trigger stops responding. |
| `style` | `FluentInfoLabelStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node for the trigger. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether the trigger takes focus on mount. |
| `onOpenChanged` | `ValueChanged<bool>?` | No | `null` | Called with the new open state whenever the tip opens or closes. |

#### State, callback, and accessibility fields

- `focusNode` (`FocusNode?`): Focus node for the trigger. One is created internally when omitted.
- `autofocus` (`bool`): Whether the trigger takes focus on mount.
- `onOpenChanged` (`ValueChanged<bool>?`): Called with the new open state whenever the tip opens or closes.

### `FluentInfoButton`

A Fluent 2 info button — the ⓘ that opens a tip beside a label.

Source: `packages/fluent_2_web/lib/src/inputs/info_button.dart`

#### Constructor: `FluentInfoButton`

```dart
const FluentInfoButton({
    super.key,
    required this.info,
    required this.semanticLabel,
    this.size = FluentInfoButtonSize.medium,
    this.icon,
    this.activeIcon,
    this.enabled = true,
    this.style,
    this.focusNode,
    this.autofocus = false,
    this.onOpenChanged,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `info` | `Widget` | Yes | — | The tip body. Wraps at 264 logical pixels. |
| `semanticLabel` | `String` | Yes | — | Announced by assistive technology. |
| `size` | `FluentInfoButtonSize` | No | `FluentInfoButtonSize.medium` | Trigger box and glyph size. |
| `icon` | `Widget?` | No | `null` | Overrides the resting glyph. Suppresses the filled swap unless [activeIcon] is also given. |
| `activeIcon` | `Widget?` | No | `null` | Overrides the hovered, pressed and open glyph. |
| `enabled` | `bool` | No | `true` | Whether the button responds to input. |
| `style` | `FluentInfoButtonStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |
| `onOpenChanged` | `ValueChanged<bool>?` | No | `null` | Called with the new open state whenever the tip opens or closes. |

#### State, callback, and accessibility fields

- `semanticLabel` (`String`): Announced by assistive technology.
- `enabled` (`bool`): Whether the button responds to input.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.
- `onOpenChanged` (`ValueChanged<bool>?`): Called with the new open state whenever the tip opens or closes.

## Related public types

### `FluentInfoButtonBaseState`

Source: `packages/fluent_2_web/lib/src/inputs/info_button.dart`

#### Constructor: `FluentInfoButtonBaseState`

```dart
const FluentInfoButtonBaseState({
    required this.enabled,
    required this.icon,
    required this.activeIcon,
    required this.info,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the trigger responds to input. |
| `icon` | `Widget` | Yes | — | The resting glyph. `Info{12,16,20}Regular` unless the caller supplied one. |
| `activeIcon` | `Widget` | Yes | — | The glyph shown while hovered, pressed or open. |
| `info` | `Widget` | Yes | — | The tip body, shown while the button is open. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the trigger responds to input.

### `FluentInfoButtonSize`

Trigger box and glyph size. Figma's `Size` axis on the `.Info button` set.

Source: `packages/fluent_2_web/lib/src/inputs/info_button.dart`

```dart
enum FluentInfoButtonSize {
  small,
  medium,
  large,
}
```

### `FluentInfoButtonState`

Source: `packages/fluent_2_web/lib/src/inputs/info_button.dart`

#### Constructor: `FluentInfoButtonState`

```dart
const FluentInfoButtonState({
    required super.enabled,
    required super.icon,
    required super.activeIcon,
    required super.info,
    required this.size,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the trigger responds to input. |
| `icon` | `Widget` | Yes | — | The resting glyph. `Info{12,16,20}Regular` unless the caller supplied one. |
| `activeIcon` | `Widget` | Yes | — | The glyph shown while hovered, pressed or open. |
| `info` | `Widget` | Yes | — | The tip body, shown while the button is open. |
| `size` | `FluentInfoButtonSize` | Yes | — | Trigger box and glyph size. |

### `FluentInfoButtonStyle`

Source: `packages/fluent_2_web/lib/src/inputs/info_button_style.dart`

#### Constructor: `FluentInfoButtonStyle`

```dart
const FluentInfoButtonStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.borderRadius,
    this.padding,
    this.iconSize,
    this.mouseCursor,
    this.infoBackgroundColor,
    this.infoForegroundColor,
    this.infoBorderColor,
    this.infoBorderWidth,
    this.infoBorderRadius,
    this.infoPadding,
    this.infoMaxWidth,
    this.infoTextStyle,
    this.infoShadow,
    this.infoOffset,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Trigger surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Info glyph colour. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Trigger corner radius. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset between the trigger's edge and the glyph box. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Glyph edge length. 12, 16 or 20, matching upstream's three icon components. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering the trigger. |
| `infoBackgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Tip surface fill. |
| `infoForegroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Tip content colour. |
| `infoBorderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Tip border colour. Null and transparent are different: `transparentStroke` becomes opaque in high contrast, where it is the only thing separating the surface from the page behind it. |
| `infoBorderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Tip border width. |
| `infoBorderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Tip corner radius. |
| `infoPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset inside the tip surface. |
| `infoMaxWidth` | `WidgetStateProperty<double?>?` | No | `null` | Width at which tip content wraps. |
| `infoTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Tip type ramp. Its colour is overridden by [infoForegroundColor]. |
| `infoShadow` | `WidgetStateProperty<List<BoxShadow>?>?` | No | `null` | Tip elevation. |
| `infoOffset` | `WidgetStateProperty<double?>?` | No | `null` | Gap between the trigger and the tip surface. |

### `FluentInfoButtonTheme`

Overrides the info button style for a subtree.

Source: `packages/fluent_2_web/lib/src/inputs/info_button.dart`

#### Constructor: `FluentInfoButtonTheme`

```dart
const FluentInfoButtonTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentInfoButtonStyle` | Yes | — | The style layered over the size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentInfoLabelBaseState`

Source: `packages/fluent_2_web/lib/src/inputs/info_label.dart`

#### Constructor: `FluentInfoLabelBaseState`

```dart
const FluentInfoLabelBaseState({
    required this.enabled,
    required this.label,
    this.infoButton,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the label renders in its enabled colour and the trigger responds to input. |
| `label` | `Widget` | Yes | — | The label slot, already built. |
| `infoButton` | `Widget?` | No | `null` | The info button slot, or null for a label with nothing to explain — which is what upstream renders when its `info` prop is absent. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the label renders in its enabled colour and the trigger responds to input.

### `FluentInfoLabelState`

Source: `packages/fluent_2_web/lib/src/inputs/info_label.dart`

#### Constructor: `FluentInfoLabelState`

```dart
const FluentInfoLabelState({
    required super.enabled,
    required super.label,
    required this.size,
    super.infoButton,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the label renders in its enabled colour and the trigger responds to input. |
| `label` | `Widget` | Yes | — | The label slot, already built. |
| `size` | `FluentLabelSize` | Yes | — | Type ramp step, shared with the label it wraps. |
| `infoButton` | `Widget?` | No | `null` | The info button slot, or null for a label with nothing to explain — which is what upstream renders when its `info` prop is absent. |

### `FluentInfoLabelStyle`

Source: `packages/fluent_2_web/lib/src/inputs/info_label_style.dart`

#### Constructor: `FluentInfoLabelStyle`

```dart
const FluentInfoLabelStyle({this.gap, this.labelStyle, this.infoButtonStyle});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the label and the info button. |
| `labelStyle` | `FluentLabelStyle?` | No | `null` | Layered over the label's own resolved style. |
| `infoButtonStyle` | `FluentInfoButtonStyle?` | No | `null` | Layered over the info button's own resolved style. |

### `FluentInfoLabelTheme`

Overrides the info label style for a subtree.

Source: `packages/fluent_2_web/lib/src/inputs/info_label.dart`

#### Constructor: `FluentInfoLabelTheme`

```dart
const FluentInfoLabelTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentInfoLabelStyle` | Yes | — | The style layered over the defaults, including the two nested slot styles. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentLabelSize`

Label type ramp step. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/inputs/label.dart`

```dart
enum FluentLabelSize {
  small,
  medium,
  large,
}
```

### `FluentLabelWeight`

Label font weight. Figma's `Type` axis, values verbatim.

Source: `packages/fluent_2_web/lib/src/inputs/label.dart`

```dart
enum FluentLabelWeight {
  regular,
  semibold,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentInfoButtonState resolveFluentInfoButtonState({
  required Widget info,
  bool enabled = true,
  FluentInfoButtonSize size = FluentInfoButtonSize.medium,
  Widget? icon,
  Widget? activeIcon,
});

FluentInfoButtonStyle resolveFluentInfoButtonStyle(
  FluentInfoButtonState state,
  FluentThemeData theme,
);

FluentInfoLabelState resolveFluentInfoLabelState({
  required Widget label,
  bool enabled = true,
  FluentLabelSize size = FluentLabelSize.medium,
  Widget? infoButton,
});

FluentInfoLabelStyle resolveFluentInfoLabelStyle(
  FluentInfoLabelState state,
  FluentThemeData theme,
);

Widget buildFluentInfoButton(
  FluentInfoButtonBaseState state,
  FluentInfoButtonStyle style,
  Set<WidgetState> states,
);

Widget buildFluentInfoLabel(
  FluentInfoLabelBaseState state,
  FluentInfoLabelStyle style,
  Set<WidgetState> states,
);

Widget buildFluentInfoTip(
  FluentInfoButtonBaseState state,
  FluentInfoButtonStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/field_stories.dart`:

```dart
FluentInfoLabel(
      info: Text('Deleted items are kept for 30 days, then purged.'),
      infoSemanticLabel: 'About the retention period',
      child: Text('Retention period'),
    )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/inputs/info_label.dart`, `packages/fluent_2_web/lib/src/inputs/info_button.dart`
- Tests: `packages/fluent_2_web/test/goldens/info_label_golden_test.dart`, `packages/fluent_2_web/test/inputs/info_button_test.dart`, `packages/fluent_2_web/test/inputs/info_label_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/field_stories.dart`, `packages/fluent_2_web/example/lib/stories/info_label_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/infolabel/usage/
- Design decisions: `references/components-actions-inputs.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
