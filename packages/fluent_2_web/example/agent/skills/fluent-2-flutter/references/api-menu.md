# Menu: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/menu/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentMenu`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentMenu`

A Fluent 2 menu.

Source: `packages/fluent_2_web/lib/src/overlays/menu.dart`

#### Constructor: `FluentMenu`

```dart
const FluentMenu({
    super.key,
    required this.items,
    required this.builder,
    this.style,
    this.itemStyle,
    this.hoverDelay = fluentMenuHoverDelay,
    this.semanticLabel,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `items` | `List<FluentMenuItem>` | Yes | — | The rows, in order. An empty list means the menu never opens. |
| `builder` | `FluentMenuTriggerBuilder` | Yes | — | Builds the widget the menu hangs off. |
| `style` | `FluentMenuStyle?` | No | `null` | Surface overrides layered over the theme defaults. Merged last, so it wins. |
| `itemStyle` | `FluentMenuItemStyle?` | No | `null` | Row overrides layered over the theme defaults. Merged last, so it wins. |
| `hoverDelay` | `Duration` | No | `fluentMenuHoverDelay` | How long a pointer rests on a row before its submenu opens. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the menu's own name. |

#### State, callback, and accessibility fields

- `semanticLabel` (`String?`): Announced by assistive technology as the menu's own name.

## Related public types

### `FluentMenuBaseState`

Source: `packages/fluent_2_web/lib/src/overlays/menu.dart`

#### Constructor: `FluentMenuBaseState`

```dart
const FluentMenuBaseState({required this.children});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `children` | `List<Widget>` | Yes | — | The rows, in order. Already built — the surface only stacks and insets them. |

### `FluentMenuItem`

Source: `packages/fluent_2_web/lib/src/overlays/menu_item.dart`

#### Constructor: `FluentMenuItem`

```dart
const FluentMenuItem({
    required this.label,
    this.icon,
    this.secondary,
    this.trailing,
    this.checked = false,
    this.selected = false,
    this.enabled = true,
    this.submenu = const <FluentMenuItem>[],
    this.onPressed,
    this.text,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `label` | `Widget` | Yes | — | The row's primary line. |
| `icon` | `Widget?` | No | `null` | Optional leading glyph, rendered at `iconSize`. |
| `secondary` | `Widget?` | No | `null` | Optional second line under [label], on the `caption2` ramp. |
| `trailing` | `Widget?` | No | `null` | Optional trailing content — normally a keyboard shortcut. |
| `checked` | `bool` | No | `false` | Whether the checkmark glyph is painted. |
| `selected` | `bool` | No | `false` | Whether the row paints Fluent's `Selected` fill. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. |
| `submenu` | `List<FluentMenuItem>` | No | `const <FluentMenuItem>[]` | Rows of the submenu this one opens, or empty for a leaf. |
| `onPressed` | `VoidCallback?` | No | `null` | Invoked when the row is activated. Ignored when [submenu] is non-empty — such a row opens its submenu instead. |
| `text` | `String?` | No | `null` | Plain text for typeahead. |

#### Constructor: `FluentMenuItem.header`

```dart
const FluentMenuItem.header({
    required this.label,
    this.text,
    this.enabled = true,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `label` | `Widget` | Yes | — | The row's primary line. |
| `text` | `String?` | No | `null` | Plain text for typeahead. |
| `enabled` | `bool` | No | `true` | Whether the row responds to input. |

#### Constructor: `FluentMenuItem.divider`

```dart
const FluentMenuItem.divider();
```

#### State, callback, and accessibility fields

- `checked` (`bool`): Whether the checkmark glyph is painted.
- `selected` (`bool`): Whether the row paints Fluent's `Selected` fill.
- `enabled` (`bool`): Whether the row responds to input.
- `onPressed` (`VoidCallback?`): Invoked when the row is activated. Ignored when [submenu] is non-empty — such a row opens its submenu instead.

### `FluentMenuItemBaseState`

Source: `packages/fluent_2_web/lib/src/overlays/menu_item.dart`

#### Constructor: `FluentMenuItemBaseState`

```dart
const FluentMenuItemBaseState({
    required this.enabled,
    required this.divider,
    required this.showCheckmark,
    required this.reserveLeading,
    required this.hasSubmenu,
    required this.label,
    this.icon,
    this.secondary,
    this.trailing,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `divider` | `bool` | Yes | — | Whether this row is a rule rather than a row of content. |
| `showCheckmark` | `bool` | Yes | — | Whether the checkmark glyph is painted. |
| `reserveLeading` | `bool` | Yes | — | Whether the leading slot is laid out even when nothing is painted. |
| `hasSubmenu` | `bool` | Yes | — | Whether the submenu chevron is painted. |
| `label` | `Widget` | Yes | — | The primary line. |
| `icon` | `Widget?` | No | `null` | The leading glyph, if any. |
| `secondary` | `Widget?` | No | `null` | The second line, if any. |
| `trailing` | `Widget?` | No | `null` | The trailing content, if any. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the row responds to input.

### `FluentMenuItemState`

Source: `packages/fluent_2_web/lib/src/overlays/menu_item.dart`

#### Constructor: `FluentMenuItemState`

```dart
const FluentMenuItemState({
    required super.enabled,
    required super.divider,
    required super.showCheckmark,
    required super.reserveLeading,
    required super.hasSubmenu,
    required super.label,
    required this.type,
    super.icon,
    super.secondary,
    super.trailing,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the row responds to input. |
| `divider` | `bool` | Yes | — | Whether this row is a rule rather than a row of content. |
| `showCheckmark` | `bool` | Yes | — | Whether the checkmark glyph is painted. |
| `reserveLeading` | `bool` | Yes | — | Whether the leading slot is laid out even when nothing is painted. |
| `hasSubmenu` | `bool` | Yes | — | Whether the submenu chevron is painted. |
| `label` | `Widget` | Yes | — | The primary line. |
| `type` | `FluentMenuItemType` | Yes | — | What this row is. |
| `icon` | `Widget?` | No | `null` | The leading glyph, if any. |
| `secondary` | `Widget?` | No | `null` | The second line, if any. |
| `trailing` | `Widget?` | No | `null` | The trailing content, if any. |

### `FluentMenuItemStyle`

Source: `packages/fluent_2_web/lib/src/overlays/menu_item_style.dart`

#### Constructor: `FluentMenuItemStyle`

```dart
const FluentMenuItemStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.secondaryColor,
    this.iconColor,
    this.checkmarkColor,
    this.dividerColor,
    this.dividerThickness,
    this.borderRadius,
    this.textStyle,
    this.secondaryTextStyle,
    this.padding,
    this.labelPadding,
    this.gap,
    this.iconSize,
    this.checkmarkSize,
    this.minimumSize,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Row fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Label colour. Also drives the trailing shortcut text. |
| `secondaryColor` | `WidgetStateProperty<Color?>?` | No | `null` | Secondary line colour. |
| `iconColor` | `WidgetStateProperty<Color?>?` | No | `null` | Leading icon colour. Brand on hover and press, unlike the label's ramp. |
| `checkmarkColor` | `WidgetStateProperty<Color?>?` | No | `null` | Checkmark colour. A brand-compound ramp, not the label's. |
| `dividerColor` | `WidgetStateProperty<Color?>?` | No | `null` | Rule colour on a `FluentMenuItem.divider` row. |
| `dividerThickness` | `WidgetStateProperty<double?>?` | No | `null` | Rule thickness on a `FluentMenuItem.divider` row. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [foregroundColor]. |
| `secondaryTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Secondary line text style. Its colour is overridden by [secondaryColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the row. |
| `labelPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Extra inset around the label column, inside [padding]. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the row's slots. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Leading icon and submenu chevron edge length. |
| `checkmarkSize` | `WidgetStateProperty<double?>?` | No | `null` | Checkmark edge length. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum row size. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering. |

### `FluentMenuState`

Source: `packages/fluent_2_web/lib/src/overlays/menu.dart`

#### Constructor: `FluentMenuState`

```dart
const FluentMenuState({required super.children, required this.custom});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `children` | `List<Widget>` | Yes | — | The rows, in order. Already built — the surface only stacks and insets them. |
| `custom` | `bool` | Yes | — | Whether the surface holds arbitrary content rather than menu rows. |

### `FluentMenuStyle`

Source: `packages/fluent_2_web/lib/src/overlays/menu_style.dart`

#### Constructor: `FluentMenuStyle`

```dart
const FluentMenuStyle({
    this.backgroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.padding,
    this.gap,
    this.minWidth,
    this.maxWidth,
    this.maxHeight,
    this.offset,
    this.shadow,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast, and on a menu it is the only thing separating the surface from the page there. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Corner radius. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding between the border and the rows. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Vertical space between rows. |
| `minWidth` | `WidgetStateProperty<double?>?` | No | `null` | The surface's minimum width. |
| `maxWidth` | `WidgetStateProperty<double?>?` | No | `null` | Width at which the surface stops growing. |
| `maxHeight` | `WidgetStateProperty<double?>?` | No | `null` | Height at which the rows start scrolling. |
| `offset` | `WidgetStateProperty<double?>?` | No | `null` | Distance between the anchor and the surface, along the opening direction. |
| `shadow` | `WidgetStateProperty<List<BoxShadow>?>?` | No | `null` | Elevation shadow. |

### `FluentMenuTheme`

Overrides the menu surface style for a subtree.

Source: `packages/fluent_2_web/lib/src/overlays/menu.dart`

#### Constructor: `FluentMenuTheme`

```dart
const FluentMenuTheme({super.key, required this.style, required super.child});
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentMenuStyle` | Yes | — | The style layered over the theme defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentMenuState resolveFluentMenuState({
  required List<Widget> children,
  bool custom = false,
});

FluentMenuStyle resolveFluentMenuStyle(
  FluentMenuState state,
  FluentThemeData theme,
);

Widget buildFluentMenu(
  FluentMenuBaseState state,
  FluentMenuStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/stories/button_stories.dart`:

```dart
FluentMenu(
      semanticLabel: 'Sharing options',
      items: _shareItems((label) => setState(() => _chosen = label)),
      builder: (context, toggle) => FluentButton(
        appearance: widget.appearance,
        size: widget.size,
        icon: fluentMenuChevron,
        iconPosition: FluentButtonIconPosition.after,
        onPressed: toggle,
        child: const Text('Share'),
      ),
    )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/overlays/menu.dart`
- Tests: `packages/fluent_2_web/test/goldens/menu_golden_test.dart`, `packages/fluent_2_web/test/overlays/menu_test.dart`
- Stories: `packages/fluent_2_web/example/lib/stories/button_stories.dart`, `packages/fluent_2_web/example/lib/stories/menu_stories.dart`, `packages/fluent_2_web/example/lib/stories/split_button_stories.dart`, `packages/fluent_2_web/example/lib/stories/toolbar_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/menu/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
