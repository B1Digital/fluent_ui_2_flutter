# Dialog: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/dialog/usage/

Design decisions and best practices: [components-surfaces-feedback.md](./components-surfaces-feedback.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentDialog`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentDialog`

A Fluent 2 dialog.

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

#### Constructor: `FluentDialog`

```dart
const FluentDialog({
    super.key,
    required this.child,
    required this.content,
    required this.open,
    this.onOpenChange,
    this.title,
    this.actions = const <Widget>[],
    this.secondaryActions = const <Widget>[],
    this.showCloseButton = true,
    this.modalType = FluentDialogModalType.modal,
    this.size = FluentDialogSize.medium,
    this.style,
    this.semanticLabel,
    this.closeButtonSemanticLabel = 'Close',
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget` | Yes | — | The trigger. Stays in the tree whether the dialog is open or not. |
| `content` | `Widget` | Yes | — | The body. |
| `open` | `bool` | Yes | — | Whether the dialog is showing. Controlled — see [onOpenChange]. |
| `onOpenChange` | `ValueChanged<bool>?` | No | `null` | Invoked with false when the user asks to close. |
| `title` | `Widget?` | No | `null` | The heading. Rendered in `subtitle1`. |
| `actions` | `List<Widget>` | No | `const <Widget>[]` | The primary actions, at the end of the footer row. |
| `secondaryActions` | `List<Widget>` | No | `const <Widget>[]` | The secondary actions, at the start of the footer row. |
| `showCloseButton` | `bool` | No | `true` | Whether the header carries a close button. |
| `modalType` | `FluentDialogModalType` | No | `FluentDialogModalType.modal` | Whether the dialog blocks the page, and how it may be dismissed. |
| `size` | `FluentDialogSize` | No | `FluentDialogSize.medium` | Width and action layout. |
| `style` | `FluentDialogStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `semanticLabel` | `String?` | No | `null` | Announced by assistive technology as the dialog's name. |
| `closeButtonSemanticLabel` | `String` | No | `'Close'` | Announced for the header's close button, which has no text of its own. |

#### State, callback, and accessibility fields

- `open` (`bool`): Whether the dialog is showing. Controlled — see [onOpenChange].
- `onOpenChange` (`ValueChanged<bool>?`): Invoked with false when the user asks to close.
- `semanticLabel` (`String?`): Announced by assistive technology as the dialog's name.

## Related public types

### `FluentDialogBaseState`

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

#### Constructor: `FluentDialogBaseState`

```dart
const FluentDialogBaseState({
    required this.content,
    required this.actions,
    required this.secondaryActions,
    required this.stackActions,
    this.title,
    this.closeButton,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `content` | `Widget` | Yes | — | The body. |
| `actions` | `List<Widget>` | Yes | — | The primary actions, laid out at the end of the row. |
| `secondaryActions` | `List<Widget>` | Yes | — | The secondary actions, laid out at the start of the row. |
| `stackActions` | `bool` | Yes | — | Whether the actions stack into a full-width column. |
| `title` | `Widget?` | No | `null` | The heading, or null for a dialog with no title. |
| `closeButton` | `Widget?` | No | `null` | The dismiss affordance in the header, or null when there is none. |

### `FluentDialogModalType`

Whether a dialog blocks the page, and how it may be dismissed.

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

```dart
enum FluentDialogModalType {
  modal,
  alert,
  nonModal,
}
```

### `FluentDialogSize`

How much of the screen a dialog claims, and how its actions stack.

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

```dart
enum FluentDialogSize {
  small,
  medium,
}
```

### `FluentDialogState`

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

#### Constructor: `FluentDialogState`

```dart
const FluentDialogState({
    required super.content,
    required super.actions,
    required super.secondaryActions,
    required super.stackActions,
    required this.size,
    required this.modalType,
    super.title,
    super.closeButton,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `content` | `Widget` | Yes | — | The body. |
| `actions` | `List<Widget>` | Yes | — | The primary actions, laid out at the end of the row. |
| `secondaryActions` | `List<Widget>` | Yes | — | The secondary actions, laid out at the start of the row. |
| `stackActions` | `bool` | Yes | — | Whether the actions stack into a full-width column. |
| `size` | `FluentDialogSize` | Yes | — | Width and action layout. |
| `modalType` | `FluentDialogModalType` | Yes | — | Whether the dialog blocks the page, and how it may be dismissed. |
| `title` | `Widget?` | No | `null` | The heading, or null for a dialog with no title. |
| `closeButton` | `Widget?` | No | `null` | The dismiss affordance in the header, or null when there is none. |

### `FluentDialogStyle`

Source: `packages/fluent_2_web/lib/src/overlays/dialog_style.dart`

#### Constructor: `FluentDialogStyle`

```dart
const FluentDialogStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.borderColor,
    this.borderWidth,
    this.borderRadius,
    this.scrimColor,
    this.shadow,
    this.padding,
    this.gap,
    this.headerGap,
    this.closeButtonPadding,
    this.actionsPadding,
    this.actionsGap,
    this.titleTextStyle,
    this.bodyTextStyle,
    this.maxWidth,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Title and body colour. |
| `borderColor` | `WidgetStateProperty<Color?>?` | No | `null` | Surface border colour. Null and transparent are different: Fluent's `transparentStroke` becomes opaque in high contrast, where it is the only thing separating the surface from the scrim behind it. |
| `borderWidth` | `WidgetStateProperty<double?>?` | No | `null` | Surface border width. Zero means no border, which is not the same as a transparent one — a zero-width border cannot become visible in high contrast. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Surface corner radius. |
| `scrimColor` | `WidgetStateProperty<Color?>?` | No | `null` | The full-viewport backdrop behind a modal or alert dialog. |
| `shadow` | `WidgetStateProperty<List<BoxShadow>?>?` | No | `null` | Surface elevation. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset between the surface border and its content. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the header, the body and the actions. |
| `headerGap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the title and the close button. |
| `closeButtonPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Extra inset before the close button, on top of [headerGap]. |
| `actionsPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset above the actions row, on top of [gap]. |
| `actionsGap` | `WidgetStateProperty<double?>?` | No | `null` | Space between adjacent action buttons. |
| `titleTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Title text style. Its colour is overridden by [foregroundColor]. |
| `bodyTextStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Body text style. Its colour is overridden by [foregroundColor]. |
| `maxWidth` | `WidgetStateProperty<double?>?` | No | `null` | Width the surface grows to, viewport permitting. |

### `FluentDialogTheme`

Overrides the dialog style for a subtree.

Source: `packages/fluent_2_web/lib/src/overlays/dialog.dart`

#### Constructor: `FluentDialogTheme`

```dart
const FluentDialogTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentDialogStyle` | Yes | — | The style layered over the size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentDialogState resolveFluentDialogState({
  required Widget content,
  FluentDialogSize size = FluentDialogSize.medium,
  FluentDialogModalType modalType = FluentDialogModalType.modal,
  List<Widget> actions = const <Widget>[],
  List<Widget> secondaryActions = const <Widget>[],
  Widget? title,
  Widget? closeButton,
});

FluentDialogStyle resolveFluentDialogStyle(
  FluentDialogState state,
  FluentThemeData theme,
);

Widget buildFluentDialog(
  FluentDialogBaseState state,
  FluentDialogStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/storybook/components/overlays_stories.dart`:

```dart
FluentDialog(
        open: open,
        onOpenChange: (value) {},
        title: const Text('Order lunch'),
        content: const Text(
          'You should eat more vegetables. '
          'Have you had a salad today?',
        ),
        actions: [
          FluentButton(
            appearance: FluentButtonAppearance.primary,
            onPressed: () {},
            child: const Text('Order'),
          ),
          FluentButton(onPressed: () {}, child: const Text('Cancel')),
        ],
        child: const Center(
          child: SizedBox(
            width: 120,
            child: FluentButton(
              onPressed: null,
              child: Text('Open the dialog via the knob'),
            ),
          ),
        ),
      )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/overlays/dialog.dart`
- Tests: `packages/fluent_2_web/test/goldens/dialog_golden_test.dart`, `packages/fluent_2_web/test/overlays/dialog_test.dart`
- Stories: `packages/fluent_2_web/example/lib/storybook/components/overlays_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/dialog/usage/
- Design decisions: `references/components-surfaces-feedback.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
