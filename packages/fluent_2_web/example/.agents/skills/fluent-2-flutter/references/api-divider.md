# Divider: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/divider/usage/

Design decisions and best practices: [components-surfaces-feedback.md](./components-surfaces-feedback.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentDivider`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentDivider`

A Fluent 2 divider: a hairline rule, optionally interrupted by a label.

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

#### Constructor: `FluentDivider`

```dart
const FluentDivider({
    super.key,
    this.child,
    this.icon,
    this.appearance = FluentDividerAppearance.standard,
    this.alignment = FluentDividerAlignment.center,
    this.vertical = false,
    this.inset = false,
    this.style,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `child` | `Widget?` | No | `null` | The label sitting between the two rules. Null for a plain rule. |
| `icon` | `Widget?` | No | `null` | Optional icon, placed before the label in reading order. |
| `appearance` | `FluentDividerAppearance` | No | `FluentDividerAppearance.standard` | How strongly the rule and label read. |
| `alignment` | `FluentDividerAlignment` | No | `FluentDividerAlignment.center` | Where the label sits along the rule. Ignored when there is no label. |
| `vertical` | `bool` | No | `false` | Whether the rule runs top to bottom rather than left to right. |
| `inset` | `bool` | No | `false` | Whether the whole divider is inset from its container. |
| `style` | `FluentDividerStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |

## Related public types

### `FluentDividerAlignment`

Where the label sits along the divider. Figma's `Layout` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

```dart
enum FluentDividerAlignment {
  start,
  center,
  end,
}
```

### `FluentDividerAppearance`

How strongly a divider reads against its surface.

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

```dart
enum FluentDividerAppearance {
  standard,
  subtle,
  strong,
  brand,
}
```

### `FluentDividerBaseState`

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

#### Constructor: `FluentDividerBaseState`

```dart
const FluentDividerBaseState({
    required this.vertical,
    required this.alignment,
    this.icon,
    this.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `vertical` | `bool` | Yes | — | Whether the rule runs top to bottom rather than left to right. Figma's `Vertical` axis. |
| `alignment` | `FluentDividerAlignment` | Yes | — | Where the label sits along the rule. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A divider with neither icon nor label is a single uninterrupted rule. |

### `FluentDividerState`

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

#### Constructor: `FluentDividerState`

```dart
const FluentDividerState({
    required super.vertical,
    required super.alignment,
    required this.appearance,
    required this.inset,
    super.icon,
    super.label,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `vertical` | `bool` | Yes | — | Whether the rule runs top to bottom rather than left to right. Figma's `Vertical` axis. |
| `alignment` | `FluentDividerAlignment` | Yes | — | Where the label sits along the rule. |
| `appearance` | `FluentDividerAppearance` | Yes | — | How strongly the rule and label read. |
| `inset` | `bool` | Yes | — | Whether the whole divider is inset from its container. Selects the `Inset` mode of Figma's `Divider padding` collection over the default `Full`. |
| `icon` | `Widget?` | No | `null` | The icon, if any. |
| `label` | `Widget?` | No | `null` | The label, if any. A divider with neither icon nor label is a single uninterrupted rule. |

### `FluentDividerStyle`

Source: `packages/fluent_2_web/lib/src/surfaces/divider_style.dart`

#### Constructor: `FluentDividerStyle`

```dart
const FluentDividerStyle({
    this.lineColor,
    this.lineThickness,
    this.shortLineLength,
    this.contentColor,
    this.textStyle,
    this.padding,
    this.contentPadding,
    this.contentMinimumSize,
    this.gap,
    this.iconSize,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `lineColor` | `WidgetStateProperty<Color?>?` | No | `null` | The rule's colour. Figma binds this to the `Stroke color` variable of the `Divider appearance` collection. |
| `lineThickness` | `WidgetStateProperty<double?>?` | No | `null` | The rule's thickness across the divider — height when horizontal, width when vertical. |
| `shortLineLength` | `WidgetStateProperty<double?>?` | No | `null` | Length of the rule on the side the label is aligned to. |
| `contentColor` | `WidgetStateProperty<Color?>?` | No | `null` | The label and icon colour. Figma's `Content color` variable. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Label text style. Its colour is overridden by [contentColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Inset applied outside both rules — Figma's `Divider padding` collection, which has exactly two modes, `Full` (zero) and `Inset`. |
| `contentPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | The gap the label box opens between itself and each rule. |
| `contentMinimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum size of the label box. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between icon and label. |
| `iconSize` | `WidgetStateProperty<double?>?` | No | `null` | Icon edge length. |

### `FluentDividerTheme`

Overrides the divider style for a subtree.

Source: `packages/fluent_2_web/lib/src/surfaces/divider.dart`

#### Constructor: `FluentDividerTheme`

```dart
const FluentDividerTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentDividerStyle` | Yes | — | The style layered over the appearance and inset defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentDividerState resolveFluentDividerState({
  bool vertical = false,
  bool inset = false,
  FluentDividerAlignment alignment = FluentDividerAlignment.center,
  FluentDividerAppearance appearance = FluentDividerAppearance.standard,
  Widget? icon,
  Widget? label,
});

FluentDividerStyle resolveFluentDividerStyle(
  FluentDividerState state,
  FluentThemeData theme,
);

Widget buildFluentDivider(
  FluentDividerBaseState state,
  FluentDividerStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/storybook/components/surfaces_stories.dart`:

```dart
FluentDivider(child: Text('OR'))
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/surfaces/divider.dart`
- Tests: `packages/fluent_2_web/test/goldens/divider_golden_test.dart`, `packages/fluent_2_web/test/navigation/nav_test.dart`, `packages/fluent_2_web/test/navigation/toolbar_test.dart`, `packages/fluent_2_web/test/surfaces/divider_test.dart`
- Stories: `packages/fluent_2_web/example/lib/storybook/components/surfaces_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/divider/usage/
- Design decisions: `references/components-surfaces-feedback.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
