# Accordion: Flutter API and field definitions

Generated by `scripts/generate-component-api.mjs`. Do not edit this file
manually; refresh it from the current Dart source and review the diff.

Official guidance: https://fluent2.microsoft.design/components/web/react/core/accordion/usage/

Design decisions and best practices: [components-navigation-data.md](./components-navigation-data.md)

## Contents

- Mapping and API rule
- Widget constructors and fields
- Related public types
- Verified usage
- Source and test evidence

## Mapping and API rule

Status: **implemented**. A purpose-built public Flutter widget exists. Use only the constructors documented below.

Mapped Flutter API: `FluentAccordion`, `FluentAccordionItem`.

The signatures below are extracted from the checked-out Dart source. React
properties from the Microsoft site are design evidence, not Flutter fields.

## Widget constructors and fields

### `FluentAccordion`

A Fluent 2 accordion: a stack of headers, each revealing a panel.

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

#### Constructor: `FluentAccordion`

```dart
const FluentAccordion({
    super.key,
    required this.children,
    this.multiple = false,
    this.collapsible = false,
    this.openItems,
    this.defaultOpenItems = const <Object>{},
    this.onToggle,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `children` | `List<Widget>` | Yes | — | The items, in order. |
| `multiple` | `bool` | No | `false` | Whether more than one panel may be open at once. |
| `collapsible` | `bool` | No | `false` | Whether the last open panel may be closed, leaving none open. |
| `openItems` | `Set<Object>?` | No | `null` | The open set, when the caller owns it. Null leaves the accordion uncontrolled. |
| `defaultOpenItems` | `Set<Object>` | No | `const <Object>{}` | The initial open set while uncontrolled. Ignored once [openItems] is given. |
| `onToggle` | `ValueChanged<Set<Object>>?` | No | `null` | Called with the set the accordion is moving to, before it is applied. |

#### State, callback, and accessibility fields

- `openItems` (`Set<Object>?`): The open set, when the caller owns it. Null leaves the accordion uncontrolled.
- `onToggle` (`ValueChanged<Set<Object>>?`): Called with the set the accordion is moving to, before it is applied.

### `FluentAccordionItem`

One header-and-panel pair inside a [FluentAccordion].

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

#### Constructor: `FluentAccordionItem`

```dart
const FluentAccordionItem({
    super.key,
    required this.value,
    required this.header,
    required this.child,
    this.icon,
    this.size = FluentAccordionSize.medium,
    this.expandIconPosition = FluentAccordionExpandIconPosition.start,
    this.enabled = true,
    this.style,
    this.focusNode,
    this.autofocus = false,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `value` | `Object` | Yes | — | Identifies this item in the accordion's open set. |
| `header` | `Widget` | Yes | — | The header label. |
| `child` | `Widget` | Yes | — | The panel content, revealed while the item is expanded. |
| `icon` | `Widget?` | No | `null` | Optional icon between the chevron and the label. |
| `size` | `FluentAccordionSize` | No | `FluentAccordionSize.medium` | Header height and type ramp. |
| `expandIconPosition` | `FluentAccordionExpandIconPosition` | No | `FluentAccordionExpandIconPosition.start` | Which side of the header the chevron sits on. |
| `enabled` | `bool` | No | `true` | Whether the header responds to input. |
| `style` | `FluentAccordionItemStyle?` | No | `null` | Overrides layered over the theme defaults. Merged last, so it wins. |
| `focusNode` | `FocusNode?` | No | `null` | Focus node to use. One is created internally when omitted. |
| `autofocus` | `bool` | No | `false` | Whether to take focus on mount. |

#### State, callback, and accessibility fields

- `value` (`Object`): Identifies this item in the accordion's open set.
- `enabled` (`bool`): Whether the header responds to input.
- `focusNode` (`FocusNode?`): Focus node to use. One is created internally when omitted.
- `autofocus` (`bool`): Whether to take focus on mount.

## Related public types

### `FluentAccordionExpandIconPosition`

Which side of the header the chevron sits on. Figma's `Chevron` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

```dart
enum FluentAccordionExpandIconPosition {
  start,
  end,
}
```

### `FluentAccordionItemBaseState`

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

#### Constructor: `FluentAccordionItemBaseState`

```dart
const FluentAccordionItemBaseState({
    required this.enabled,
    required this.expanded,
    required this.expandIconPosition,
    this.icon,
    this.header,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the header responds to input. |
| `expanded` | `bool` | Yes | — | Whether the panel is open. Figma's `Expanded` axis. |
| `expandIconPosition` | `FluentAccordionExpandIconPosition` | Yes | — | Which side the chevron sits on. |
| `icon` | `Widget?` | No | `null` | Optional icon between the chevron and the label. |
| `header` | `Widget?` | No | `null` | The header label, if any. |

#### State, callback, and accessibility fields

- `enabled` (`bool`): Whether the header responds to input.
- `expanded` (`bool`): Whether the panel is open. Figma's `Expanded` axis.

### `FluentAccordionItemState`

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

#### Constructor: `FluentAccordionItemState`

```dart
const FluentAccordionItemState({
    required super.enabled,
    required super.expanded,
    required super.expandIconPosition,
    required this.size,
    super.icon,
    super.header,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `enabled` | `bool` | Yes | — | Whether the header responds to input. |
| `expanded` | `bool` | Yes | — | Whether the panel is open. Figma's `Expanded` axis. |
| `expandIconPosition` | `FluentAccordionExpandIconPosition` | Yes | — | Which side the chevron sits on. |
| `size` | `FluentAccordionSize` | Yes | — | Header height and type ramp. |
| `icon` | `Widget?` | No | `null` | Optional icon between the chevron and the label. |
| `header` | `Widget?` | No | `null` | The header label, if any. |

### `FluentAccordionItemStyle`

Source: `packages/fluent_2_web/lib/src/surfaces/accordion_style.dart`

#### Constructor: `FluentAccordionItemStyle`

```dart
const FluentAccordionItemStyle({
    this.backgroundColor,
    this.foregroundColor,
    this.borderRadius,
    this.textStyle,
    this.padding,
    this.gap,
    this.expandIconSize,
    this.minimumSize,
    this.panelPadding,
    this.mouseCursor,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `backgroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Header surface fill. |
| `foregroundColor` | `WidgetStateProperty<Color?>?` | No | `null` | Header label, leading icon and chevron colour. |
| `borderRadius` | `WidgetStateProperty<BorderRadius?>?` | No | `null` | Header corner radius. Also the radius the focus ring is drawn on. |
| `textStyle` | `WidgetStateProperty<TextStyle?>?` | No | `null` | Header label text style. Its colour is overridden by [foregroundColor]. |
| `padding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding inside the header. |
| `gap` | `WidgetStateProperty<double?>?` | No | `null` | Space between the chevron, the leading icon and the label. |
| `expandIconSize` | `WidgetStateProperty<double?>?` | No | `null` | Chevron edge length. |
| `minimumSize` | `WidgetStateProperty<Size?>?` | No | `null` | Minimum header size. Fluent specifies the height only. |
| `panelPadding` | `WidgetStateProperty<EdgeInsetsGeometry?>?` | No | `null` | Padding around the panel content while it is expanded. |
| `mouseCursor` | `WidgetStateProperty<MouseCursor?>?` | No | `null` | Cursor while hovering an enabled header. |

### `FluentAccordionItemTheme`

Overrides the accordion item style for a subtree.

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

#### Constructor: `FluentAccordionItemTheme`

```dart
const FluentAccordionItemTheme({
    super.key,
    required this.style,
    required super.child,
  });
```

| Field | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `key` | `Key?` | No | `null` | Flutter widget identity. |
| `style` | `FluentAccordionItemStyle` | Yes | — | The style layered over the size defaults. |
| `child` | `Widget` | Yes | — | The widget subtree rendered or affected by this API. |

### `FluentAccordionSize`

Header height and type ramp. Figma's `Size` axis.

Source: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`

```dart
enum FluentAccordionSize {
  small,
  medium,
  large,
  extraLarge,
}
```

## Advanced public recomposition functions

Use these only when the complete widget/style/theme composition cannot
meet the requirement. Preserve semantics, focus, states, and motion.

```dart
FluentAccordionItemState resolveFluentAccordionItemState({
  bool enabled = true,
  bool expanded = false,
  FluentAccordionSize size = FluentAccordionSize.medium,
  FluentAccordionExpandIconPosition expandIconPosition =
      FluentAccordionExpandIconPosition.start,
  Widget? icon,
  Widget? header,
});

FluentAccordionItemStyle resolveFluentAccordionItemStyle(
  FluentAccordionItemState state,
  FluentThemeData theme,
);

Widget buildFluentAccordionItem(
  FluentAccordionItemBaseState state,
  FluentAccordionItemStyle style,
  Set<WidgetState> states,
);
```

## Verified usage

Checked-in usage excerpt from `packages/fluent_2_web/example/lib/storybook/components/surfaces_stories.dart`:

```dart
FluentAccordion(
          multiple: true,
          defaultOpenItems: const {'1'},
          children: const [
            FluentAccordionItem(
              value: '1',
              header: Text('General'),
              child: Padding(
                padding: EdgeInsets.all(12),
                child: Text('Your general account settings.'),
              ),
            ),
            FluentAccordionItem(
              value: '2',
              header: Text('Privacy'),
              child: Padding(
                padding: EdgeInsets.all(12),
                child: Text('Control what others can see.'),
              ),
            ),
          ],
        )
```

This excerpt verifies current constructor names. It may depend on local
variables or surrounding story/test setup; inspect the cited file before
copying it into a standalone application.

## Source and test evidence

- Implementation: `packages/fluent_2_web/lib/src/surfaces/accordion.dart`
- Tests: `packages/fluent_2_web/test/goldens/accordion_golden_test.dart`, `packages/fluent_2_web/test/surfaces/accordion_test.dart`
- Stories: `packages/fluent_2_web/example/lib/storybook/components/surfaces_stories.dart`
- Official usage: https://fluent2.microsoft.design/components/web/react/core/accordion/usage/
- Design decisions: `references/components-navigation-data.md`

If source and this snapshot differ, stop and run
`node scripts/generate-component-api.mjs --write`, review the diff, and
re-run the bundled validators before generating Flutter code.
